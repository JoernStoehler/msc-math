# Incremental route: observations and scope

Three sequential fresh-reader-context releases were used. The reader is one independently spawned agent, whose context accumulates; it is not three independent replications. It read only the released prose, not existing reviews or source packets. Full prompts and outputs are preserved. Its job was meaning reconstruction and locating missing dependencies, not predicting Jörn acceptance.

1. Prefix through pooled association: reader reconstructed mathematical/historical distinction and asked for product definition and bucket indices. Added those definitions.
2. Prefix through exact identity and adverse paths: reader reconstructed argument but requested capacity theorem locator, HKO significance and path guarantees. Added locator and HKO context; checked actual path interpretation and weakened an overstrong inference from numerical paths.
3. Complete section: reader independently observed that the initial100k maximum did not provide a control comparison, questioned within-bucket refinement and pooled mean, and could not reconstruct the transferred selector. Author retained initial study as computational allocation evidence, made refinement design explicit, and recovered transferred rule and actual bucket mean effects from the packet.

Final is a substantial complete section, not a polished isolated paragraph. It retains all mathematical formulas and empirical studies, but expands selection exposition and introduces a small comparison table. Reader comprehension was already substantially accurate on the baseline prefixes; this experiment does not establish that all identified ambiguities would trouble an expert or that every expansion earns its space. The final has not been assessed by the authoring reader, deliberately leaving prospective review separate.

## Remaining integration obligations

Theorem reference is an actual existing LaTeX label but not resolved in this Markdown artifact. Source notes are retained to locate empirical evidence and should be replaced by stable manuscript references during integration. Full distribution/generation details and held-out model metrics remain in source owners rather than expanded here. The fragment does not establish current-code certification of historical targets, nor does it explain all empirical associations mathematically. Human acceptance is untested.

## Provenance

Baseline/final hashes in manifest.json; exact additional sources in supplemental-source/ with hashes and original absolute paths. Original scientific input packet and concentration supplement remain immutable under20260918-easy-routes/. Parent instruction owns the initial task; author-protocol.md records operational choices. Parent will commit this run. Reader independently committed its first two output files as469781a8 before the no-commit reminder; that commit contains no manuscript changes.
