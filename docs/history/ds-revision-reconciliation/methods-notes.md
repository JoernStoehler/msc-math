# Methods details retained outside the candidate prose

These notes preserve details relocated from the supplied revision. They are not new calculations or a change in the experimental design. Eventual thesis integration should link them to the actual experiment methods, not leave a machine-local reference.

- Historical random sample: the supplied revision records seed 42, support heights in [0.8,1.2], rejection of geometries failing generation conditions, and balanced counts within generic facet-count and product polygon-size groups. Only seed 42 was removed from the reader prose. Generator/lineage owners: `experiments/polytope-datasets/retained-lineage.md` and `experiments/sys-datascience/README.md`.
- The 100,000-product concentration comparison used a criterion specified before capacity evaluation: positive overall mean difference and positive differences in at least seven of ten polygon-size pairs. It imposed no minimum effect size and was not a significance test. The prose reports the observed difference (about 0.034) and signs (eight of ten); it does not call this statistical significance. Owner: `experiments/sys-datascience/methods/extreme-scalar-rejection-proposer/artifacts/100k-ridge-concentration-validation/`.
- In the alternative-generator comparison, 16 controls per polygon-size group were chosen by a fixed hash ordering before capacity evaluation. The eligible control pool excluded bodies selected by either the ridge selector or the covariance selector also tested in that experiment. Thus these are not asserted to be a random sample of all generator outputs or a pure complement of only the ridge-selected set. Retained source: `experiments/sys-datascience/methods/alternative-source-transfer/{README.md,POST-TARGET-ACCOUNT.md,src/main.rs,artifacts/transfer-v1/analysis.json}`.
- Opposing product paths: `experiments/sys-datascience/methods/ridge-endpoint-path/README.md` identifies four positions (`q01`, `q001`, `q0001`, `endpoint`) in each of two rotation families. The revised sentence refers to these sampled positions, not all intermediate rotations. `INTERPRETATION.md` limits the conclusion to the retained numerical observations. No proof of monotonicity on continuous paths is introduced.

The historical-capacity provenance qualification remains in the candidate. Revalidation now underway elsewhere may change the appropriate description, but successful execution, coverage, discrepancies and guarantees must be checked before that paragraph or the numerical values are replaced.
