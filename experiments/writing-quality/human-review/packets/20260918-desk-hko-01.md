# HKO local reading sample

Context: these paragraphs surround a worked derivative calculation in a chapter about perturbing a product of two planar polygons. The first precedes the calculation; the second follows its displayed formula for DU[h]. The intervening formulas are omitted here. This review concerns these paragraphs, not validation of that calculation or the chapter.

## Before the calculation

Vary the first momentum component of the second \(q\)-facet normal. In coordinates \((q_1,q_2,p_1,p_2)\), take \(h_2=(0,0,1,0)\) and \(h_i=0\) for \(i\ne2\). This is flat coordinate 6 in the verifier’s zero-based storage. Put \(w=c+kr=\beta_{0,1}\).

## After the calculation

This is an actual coefficient of the upper-branch row for the smooth feasible-section upper bound \(U\), approximately \(-0.1801707325\); it is not an assertion about the derivative of the actual capacity. Varying the two polygon factors keeps each \(q\)-facet normal in the \(q\)-plane. The perturbation above instead adds a \(p_1\) component to one such normal, so the forty-coordinate calculation includes a direction that differentiating only within products would miss.

## Review question

Do these paragraphs read acceptably to you in this context? A brief verdict is enough; if something catches, quoting the troublesome words is useful but optional. “Need more context” is a valid outcome. Estimated effort: about one minute.

This determines whether to retain this local repair as human-acceptable prose in the repair experiment, or leave it unaccepted pending revision/context. It does not authorize manuscript edits or certify the omitted text.
