# Endpoint-vs-Random Classification

Status: no current retained-dataset evidence packet.

Dataset path: `../../dataset/` from this method folder.

Current command: none. A current rerun first needs a clean row-specific script.

The old `feature-pattern-search/` classification artifacts were deleted instead
of rescued because they depended on the same confusing local feature views and
pre-LICCA report state.

Current method-table role:

- endpoint-vs-random classification as a caveat/diagnostic row;
- metadata and provenance separation is a validity caveat, not a
  candidate-proposer;
- non-provenance table-column separation may be supporting/caveat evidence but
  still does not by itself propose new `sys > 1` candidates.

Next action:

- write a clean script only if this row remains required after the current
  method inventory.
