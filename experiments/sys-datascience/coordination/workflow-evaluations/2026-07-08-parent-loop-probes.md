# 2026-07-08 Parent-Loop Workflow Probes

Status: historical evaluation of the parent-loop material at `bb0e538e`.
The evaluated launch state and LICCA continuation are not current instructions;
the retained value is the observed bypasses and probe evidence for a half-tested
GPT-5.5 prototype. No rule from this report is thereby installed into the
current workflow, and a GPT-5.6 workflow will be designed from scratch.
Relative file references below describe the controlled `bb0e538e` tree and may
not exist in the current checkout.

Use: process evidence for the sys-datascience autonomous parent-loop material.
This file records what two read-only fresh-agent probes found and what material
changes the parent made in response. It is not evidence for any
sys-datascience mathematical or method-table claim.

## Controlled Material

Workspace:

```text
/workspaces/msc-math/.worktrees/datascience-agent-memory
```

Parent transcript source:

```text
thread id: 019f3bb9-35f8-7cc3-8a8b-27c489a898cf
rollout: /home/vscode/.codex/sessions/2026/07/07/rollout-2026-07-07T08-37-01-019f3bb9-35f8-7cc3-8a8b-27c489a898cf.jsonl
```

The probes were read-only and ran no experiments.

## Probe 1: Fresh Parent Kickoff Read

Subagent id: `019f4248-7a1c-73f0-ac0e-3173081d2f50`.

Prompt shape: `Fresh-Agent Workflow Probe` from `../prompt-templates.md`, with
the controlled workspace above.

Observed read:

- Correctly chose the autonomous parent-loop milestone as the first action.
- Did not launch a method packet, rerun scalar/ridge work, or write the bounded
  thesis story first.
- Correctly classified bounded retained wording as fallback rather than
  full-slice closure.
- Correctly identified the claim gate, packet-card gate, workflow-test
  separation, and stop-state gate.

Material weaknesses found:

- Some relative paths in `../autonomous-parent-loop.md` were misleading from
  the coordination folder.
- The first 30 minutes of parent behavior were underspecified.
- Stop-state vocabulary differed between `../autonomous-parent-loop.md` and
  `../../agent-memory-and-expansion-plan.md`.
- Workflow-test preservation had no explicit storage location.
- Minimal topic-read requirements for the current milestone were not explicit.

## Probe 2: Adversarial Bypass Review

Subagent id: `019f4248-931d-7182-9963-d86bb1f448e5`.

Prompt shape: `Adversarial Workflow Bypass Review` from
`../prompt-templates.md`, with the controlled workspace above.

Bypass risks found:

- A parent could write a good plan and stop as `milestone-complete` without
  launching or handing off the first wave.
- A parent could treat bounded fallback as full closure by its own local
  decision.
- A parent could launch a runnable standard-method packet after a shallow
  packet card with only a token alternative.
- Executor output could be accepted as thesis evidence without a separate
  review verdict.
- The material still allowed a low-value Jörn should-question before cheap
  local branch exploration.
- The method-surface claim ladder had one row that broadened retained-table
  evidence into full-slice language too easily.

## Parent Interpretation And Edits

Accepted probe findings and material edits:

- `../autonomous-parent-loop.md` now uses correct relative paths, adds a
  `First 30 Minutes` block, names minimal topic reads, and requires
  milestone-complete to include a launched next packet or concrete autonomous
  continuation prompt/owner in `../active-work.md`.
- `../../agent-memory-and-expansion-plan.md` now uses the same stop states:
  `complete / milestone-complete / loud-failure / awaiting-Jörn-crux /
  blocked`.
- Workflow-test packets now route preserved prompt, response, parent
  interpretation, and material verdicts to this `workflow-evaluations/`
  folder.
- Bounded fallback can close the full slice only after explicit Jörn or
  thesis-control downgrade; otherwise it is only fallback/source-map work.
- Execution packet cards now require exact thesis sentence or decision, source
  artifact/table, evaluation target, protocol choices, and best rejected or
  parked alternative.
- Thesis-evidence or global-belief updates now require a named review verdict
  artifact or clearly labeled review section separate from executor output.
- The Jörn-crux gate now includes the negative example of asking whether to use
  bounded fallback before cheap local exploration.
- `../topics/method-surface-expansion.md` now splits retained-scope wording
  from unlicensed full-slice wording.

Remaining risks after the initial probes, before P2 existed:

- At that point, the first execution packet after P1/P3 still needed to test
  whether the workflow can produce, review, and synthesize a real method result
  without Jörn steering.

## Focused Re-Probe After Edits

Subagent id: `019f424c-8467-7ad0-aa31-44e8dfd8cacf`.

Prompt shape: focused read-only check of the six prior bypasses against the
patched material.

Verdict:

- resolved: parent cannot stop as `milestone-complete` after only a plan/status
  report;
- resolved: parent cannot close the full slice with bounded fallback by local
  choice alone;
- resolved: standard-method baseline cannot launch after shallow planning
  without exact thesis sentence/decision, source artifact/table, evaluation
  target, protocol choices, and best rejected/parked alternative;
- resolved: executor output cannot be accepted as thesis evidence or global
  belief update without a separate review verdict artifact or clearly labeled
  review section;
- resolved: material no longer easily invites a low-value Jörn should-question
  before cheap local exploration;
- resolved: full-slice "no new source" wording is separated from retained-table
  evidence with a missing-work warning.

Remaining risk after re-probe, before P2:

- The probes checked known bypasses, not the full workflow state space.
- The P1/P3 design launch worked as a read-only wave, but the P2
  execution/review/synthesis loop is still untested.

Update after P2: P2 executed, produced artifacts, received a separate review
verdict, and was synthesized in `../p2-synthesis-2026-07-08.md`. The remaining
workflow risk is the next branch choice after P5 and any later compute packet,
not the first execution/review/synthesis loop.

## Post-Update Local Bypass Audit

Mode: local adversarial read after the bounded source-map, P4 closure, and
high-complexity compute packet were written. No subagent was spawned for this
audit under the current delegation policy.

Question: after the new artifacts, could a future parent still silently fail by
starting from stale "source-map is next" material, treating the compute packet
as evidence, or stopping after an intermediate artifact?

Findings and edits:

- Stale continuation wording remained in the autonomous parent kickoff prompt
  and in historical P5/P4 synthesis sections. Edited
  `../autonomous-parent-loop.md`, `../prompt-templates.md`,
  `../p5-mechanism-tail-thesis-use-audit-2026-07-08.md`, and
  `../p4-generated-candidate-closure-2026-07-08.md` so current readers see the
  branch point: bounded thesis prose versus LICCA execution/review of the
  prepared compute packet.
- The top-level sys-datascience README and agent memory could still imply that
  broader producer work was only a future compute-packet preparation task.
  Edited `../../README.md` and `../../agent-memory-and-expansion-plan.md` to
  say the compute packet exists but is unrun and non-evidential.
- The decision board and topic files could still invite reopening P4 or
  source-map work as if they were not already done. Edited
  `../next-session-candidates.md`,
  `../topics/generated-candidate-proposers.md`, and
  `../topics/method-surface-expansion.md`.
- The session itself exhibited the known premature-stop failure: reporting a
  useful intermediate state without a concrete blocker or request. Added
  `../process-learnings.md` entry "Intermediate Artifacts Are Not Turn
  Boundaries."
- The first high-complexity compute packet commands copied
  `produce/shared-cache.jsonl` into `--base-cache`. A local actual smoke test
  caught that this schema is wrong for `sys-datascience-produce`, which expects
  computed-polytope payload rows with `poly_id`. The packet now uses an empty
  run-local base cache and records a passing local smoke/prepare/fingerprint.
- The post-prepare scan command initially failed because `scan-sys-gt-1`
  imports shared code requiring `numpy`. The compute packet now uses
  `uv run --with numpy --script ...` for that scan.

Current remaining risk:

- The next real evidence-producing step needs LICCA access. The compute packet
  is designed to fail loudly on smoke/validation/prepare/fingerprint errors.
  Local smoke tested the producer/prepare/fingerprint path, but not the actual
  Slurm submission environment.
- The local audit is not a fresh-agent probe. If the workflow material is
  changed again before kickoff, a read-only fresh probe is still useful.
