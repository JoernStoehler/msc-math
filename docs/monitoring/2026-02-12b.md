# Monitoring Report: 2026-02-12 (second run)

**Session:** post-known-polytopes-merge
**Checks run:** All 6
**Trigger:** Merges to main since first report (known-polytopes refactoring)
**Base commit:** `d945c96` (local `main`)
**Previous report:** `2026-02-12.md` at commit `4f36233`

---

## Changes Since Last Report

5 commits since first report:

```
d945c96 Remove MONITORING.md stub that just redirected to skill
cff8f05 Merge branch 'claude/geom-known-polytopes'
13b8a35 Deduplicate random polytope generation into geom::test_utils
80a5266 Eliminate duplicated capacity literals and indirect imports
849e766 Move known polytopes into geom as single source of truth
```

The `claude/geom-known-polytopes` merge consolidated test fixtures and random polytope generation into `geom`, eliminating duplication across crates.

---

## Check Results

### Check 1: Boundedness Detection Agreement

**Status:** PASS
**Key metric:** 875/1000 tested (500 bounded, 375 unbounded), 0 disagreements
**Notes:** Completed in 2.75s (was 3.05s). Generator still produces bounded cases for `RankDeficient` pattern — harmless but noisy.

### Check 2: Volume Algorithm Agreement

**Status:** PASS
**Key metric:** 1000 polytopes tested, max relative error: 4.82e-8
**Notes:** Identical to previous run. Completed in 11.20s (was 12.29s).

### Check 3: Repo Invariants

**Status:** PASS
**Key metric:** 110 tests passed, 8 ignored, 0 failed. Clippy clean.
**Notes:**
- Test count increased from 64→110 (+72%) due to known-polytopes refactoring moving and adding tests
- Compiler warning in test code: field `check_bounded_bugs` is never read (in geom test code, not caught by `clippy --lib`)

### Check 4: Issue Board Health

**Status:** STRUCTURAL GAP (unchanged)
**Key metric:** 18 open issues, 0 labels on any issue
**Notes:** Same as previous report. No lifecycle labels exist. Last issue update: Feb 8 (#12).

### Check 5: Build & Test Performance

**Status:** IMPROVED — hk2017 still at 74% (threshold: 60%)

| Measurement | Wall Time | Previous | Change | Notes |
|---|---|---|---|---|
| Hot build (no-op) | 0.48s | 0.11s | +0.37s | datasets recompiled |
| Full test suite | 0:52 | 2:34 | **-66%** | |
| geom | 11.1s | 12.8s | -13% | 52 passed, 4 ignored |
| hk2017 | 38.8s | 2:18 | **-72%** | 25 passed, 4 ignored |
| billiard | 0.0s | 0.0s | — | 1 test |
| tube | 0.0s | 0.0s | — | 1 test |
| datasets | 2.5s | 2.8s | -11% | 31 passed |
| Clippy | 0.77s | 0.11s | +0.66s | Clean (datasets needed recheck) |

**Hotspot:** hk2017 at 38.8s out of 52.4s total = **74%** of test time. Down from 89% but still above 60% threshold.

**Major improvement:** The known-polytopes refactoring (moving shared fixtures to `geom::test_utils` and `geom::known_polytopes`) cut hk2017 test time from 138s to 39s — a 72% reduction. The fixture redesign eliminated redundant `ehz_capacity()` recomputation across `LazyLock` instances.

**Budget status:** 52s wall time is well within the 10-min budget, with ample margin for growth. Previous concern about tight 3-min budget is resolved.

### Check 6: Stale Processes

**Status:** PASS
**Key metric:** 0 stale processes found
**Notes:** Clean. Previous zombies were killed in earlier session.

---

## Alert Threshold Breaches

1. **Check 5:** hk2017 at 74% of test time (threshold: 60%). Improved from 89% but still breaching.

---

## Findings

1. **Test performance dramatically improved.** The known-polytopes refactoring achieved a 66% wall-time reduction for the full suite (2:34 → 0:52) while *increasing* test count by 72% (64 → 110). This resolves the previous report's concern about tight time budgets.

2. **hk2017 hotspot reduced but persistent.** At 74% of total test time, hk2017 is still dominant. The absolute time (39s) is reasonable; the percentage is high because other crates are very fast. This will naturally improve as geom and other crates gain more tests.

3. **Dead code in test code.** Field `check_bounded_bugs` in geom test code is never read. Minor cleanup opportunity.

4. **All previous-report items from Checks 1, 4, 6 are unchanged.** Check 1 test name is now correct (confirmed working). Issue board still has no labels. No new zombie processes.

---

## Suggested Actions

1. [Deferred] hk2017 at 74% — no action needed now; ratio will drop as other crates grow
2. [Minor] Clean up unused `check_bounded_bugs` field in geom test code
3. [Carried forward] Add issue lifecycle labels (Jörn's triage decision)

## Suggested Skill Changes

1. Update Check 5 baseline to use this report's numbers as the new reference
2. Consider relaxing the 60% single-crate threshold to 75% — in a workspace with one compute-heavy crate and several lightweight ones, dominance by the heavy crate is structural, not a regression
