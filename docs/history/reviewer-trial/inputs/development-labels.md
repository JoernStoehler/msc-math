# Contemporary DS: human review received

## Evidence and scope

Request: `20260918-root-contemporary-ds-reader`. Received directly from Jörn in this desk conversation on 2026-09-18 as a pasted Hypothesis JSON export. Exact text between his `<json>` delimiters is preserved in [20260918-root-contemporary-ds-reader.hypothesis.json](20260918-root-contemporary-ds-reader.hypothesis.json), SHA-256 `7381db4c9b1bc989f66d6b39b88587d6319b62707df35805fb6d24f5c03fbcd1`. Formatting, spelling, qualifications, selectors and timestamps are unchanged. No remote private-annotation API was accessed.

Reviewed Markdown SHA-256: `dc2f268875887cf8bfad3f513a68d3294f5652d5d689be7406a301d9ed335a36`. Rendered HTML SHA-256: `6f42e6c28b673495dcccf26342818a7cce9d30e3baee3a41adbe7e8db84caf37`. Original source: `/workspaces/msc-math/.worktrees/easy-writing-routes/experiments/writing-quality/runs/20260918-easy-routes/route-reader/final.md`. The rendered packet's scope/question framing was shown in addition to the source. Chat supplied Hypothesis instructions and a rough 8–12 minute effort estimate, but no AI issue list or verdict.

Exposure: independent of the frozen agent issue inventory in this exchange; not blind to project history, purpose or selection context. User knows this was selected as a plausible contemporary artifact. Root and desk have prior feedback exposure.

All 12 TextQuoteSelector excerpts match the indicated DOM paragraph/title after whitespace normalization. The source/rendered hashes were verified. Exported MathML text sometimes includes both rendered notation and its source annotation; retain those strings as evidence, not as alleged visible rendering defects.

Overall verdict, explicitly supplied in a subsequent message: **“Needs revision before acceptable”** for this exact sample. Local positive and qualified judgments remain intact; unmarked text is not certified clean. No manuscript edit authorized by this record.

## Human annotations, in document order

### 1. EPyd9LNLEfG5d-uwzVk5LQ

Anchor: `/main[1]/h1[1]`. Exact selection:

```text
Ridge geometry as
a guide to candidate search
```

Exact human comment:

```text
Imo not a good title. "Empirical pattern in ridge area vs systolic capacity" would communicate more clearly what we have obtained as a result (?)
```

### 2. I9NHyrNLEfGVRe_pLhef-A

Anchor: `/main[1]/p[1]`. Exact selection:

```text
Random sampling can do more than return the largest systolic ratio
encountered.
```

Exact human comment:

```text
Says very little / is too vague and thus too inefficient at communication. I guess this makes sense if the previous section was immediately about finding hte maximum sys among random samples? hmm. yeah in that case it's a good sentence suddenly bc it makes clear what direction we move in now.
```

### 3. fOd6UrNLEfGTEa9XaeJsug

Anchor: `/main[1]/p[2]`. Exact selection:

```text
The initial population comprised 4,096 generic four-dimensional
polytopes
```

Exact human comment:

```text
This is definitely not relevant to the mathematician right now? I think a paragraph on statistical significance / numerical errors / the kind of "model error" that comes from sample count rather than e.g. method or inteprretation errors, would be better, and should be later / after presenting the clear case for "what if the thing we found is taken at face value, what if our interpretation is true"
```

### 4. orWdcrNLEfGUv79EMLUpdQ

Anchor: `/main[1]/p[3]`. Exact selection:

```text
since both the face integrals and the square root
of four-dimensional volume scale quadratically.
```

Exact human comment:

```text
imo too obvious, this kind of scaling consideration is trivial to make from teh formula for a msc student.
```

### 5. s8JoyrNLEfGw2Z_VFfQUcw

Anchor: `/main[1]/p[4]`. Exact selection:

```text
targets
```

Exact human comment:

```text
ambiguous / hard to track. I think you should just say "systolic ratio" ? or systole [??]? instead of using ambiguous pronouns / synonyms / neologisms / references.
```

### 6. 8eOo-LNLEfG3ac8HmxpATg

Anchor: `/main[1]/p[4]`. Exact selection:

```text
The
Spearman rank correlation was
−0.938437-0.938437,
whereas the Pearson correlation was only
−0.205039-0.205039.
```

Exact human comment:

```text
small point here is that i don't quite epxect the imaginary reader (or even Kai irl) to be familiar with these / to interpet them well. Also style issue is the number of digits here, since they convey no information (?)

```

### 7. acOoRrNMEfGhO3cRsuRsrQ

Anchor: `/main[1]/p[5]`. Exact selection:

```text
Products make that information more concrete.
```

Exact human comment:

```text
here we definitely would need to split by subsection:
- pure empirics on random polytopes, on random product polytopes (also: figures!!)
- now a theoretical exploration that uses more "deep" math (instead of forgetting where our invariants come from at all) for the product case -- with the pentagon products in particular -- perhaps worth even first doing the pentagon case [bc it references our neat proof for the exact systolic ratio formula in dependence on theta] and only afterwards generalize [bc iiuc the generalization is limited?] ah no wait it's fine to do both, i.e. general + specialized formulas close to antoher. forget i critiqued the narration here. i think styling could be better / make it clearer that there's now a shift in method/approach bc we try to explain the pattern we saw / take it as inspiration
```

### 8. iYKd1rNMEfGhPDdKiX0ILQ

Anchor: `/main[1]/p[6]`. Exact selection:

```text
the ratio
```

Exact human comment:

```text
again problematic way to write. just say systolic ratio.
```

### 9. k6Zx_LNMEfGHgTNd4sdmJQ

Anchor: `/main[1]/p[6]`. Exact selection:

```text
This is an analytic example outside the finite random table, so its
value above one does not conflict with that table’s recorded maximum.
```

Exact human comment:

```text
this does not belong in this section at all
```

### 10. vq5IXLNMEfG2pw-j1ww_2w

Anchor: `/main[1]/p[7]`. Exact selection:

```text
Small ridge sum is therefore not a universal ascent
objective.
```

Exact human comment:

```text
nice result, unsure whether we can move this forward so that scanning the paragraphs' first sentences just reevals all the results, with arguments / details / refinements of a coarse description of the result into sth more rigorous / less misunderstandable after?
```

### 11. 7iCqsrNMEfG-LMtsXxTSFw

Anchor: `/main[1]/p[8]`. Exact selection:

```text
Candidate-selection experiments address that more limited use.
Predictive models and feature ablations found that ridge-area features
carried much of the held-out signal.
```

Exact human comment:

```text
to sloopy / weird. i think this is another subsection, which now no longer uses "dataset => find pattern" but "found pattern => create a filtered dataset to see if the pattern is retained even as we start optimizing with it" ?
```

### 12. _zopfLNMEfGsbOsvxmDZlA

Anchor: `/main[1]/p[9]`. Exact selection:

```text
Geometry-guided selection and local refinement consequently answer
different questions. The former allocates expensive evaluations among
proposed bodies; the latter tries to improve a chosen body.
```

Exact human comment:

```text
i don't think this is rounding off the chapter at all and is just irrelevant to mathematicians
```

## Desk interpretation (not additional human labels)

- The title should communicate a result more directly. The proposed title is tentative; do not silently substitute “capacity” or “systole” for the mathematical systolic ratio based on speculative alternatives in comments.
- The opening objection is context-dependent: Jörn reconsidered it as a good transition if the preceding section concerns maximum systolic ratio among random samples. Do not record an unconditional rejection.
- Present the phenomenon/interpretation before detailed population and uncertainty discussion. This is a placement and emphasis objection, not permission to conceal numerical limitations or to invent statistical significance.
- Avoid explaining scaling he considers immediate for the mathematical audience. Use explicit, stable quantity names rather than “targets” or ambiguous “the ratio.”
- Explain unfamiliar correlation measures and use informative precision; his digits objection is tentative.
- Make empirical discovery, theoretical interpretation and subsequent selection experiments visibly distinct, including figures for empirics. He explicitly withdrew the proposed pentagon-first/general-later ordering objection; retain his request to signal the change of method.
- Remove or relocate the analytic-example/random-table non-conflict aside; the marked statement is judged out of place, not mathematically false.
- He positively calls the non-universal-ascent finding a nice result and tentatively proposes result-first paragraph openings followed by arguments/refinements.
- Selection experiments need a distinct, clearer account of using a discovered pattern to filter new data and examine its persistence. Treat his proposed interpretation as a question requiring source checking, not an established claim that every experiment measured that.
- The marked closing selection/local-refinement distinction does not provide a useful mathematical conclusion for him. His annotation spans the first two closing sentences; it is not an explicit rejection of every remaining sentence.

## Comparison with frozen agent audit (desk assessment)

Compared only the Candidate D portion and relevant shared conclusions of `/workspaces/msc-math/.worktrees/easy-writing-routes/experiments/writing-quality/runs/20260918-easy-routes/evaluation/source-review.md`; hash `8fdd79f4515cf17bac155978e21d21a07089d1b5c9e770df67b860faaeeb0dbc` verified before reading. The audit was frozen before this human feedback and describes itself as scientific/comprehension auditing, not acceptance prediction.

| Topic | Agent audit | Human feedback / relation |
|---|---|---|
| Selection paragraph | Notes three experiments packed into a dense paragraph; distinctions present. | Partial overlap. Jörn requests a separate subsection and clearer methodological progression. Density recognition is narrower. |
| Exact example versus table maximum | Praises the separation as an accurate scientific boundary. | Jörn says the explicit non-conflict aside does not belong here. Truth and expositional usefulness differ. |
| Historical target distinction | Calls the distinction explicit and scientifically appropriate. | Jörn objects to “targets” and other indirect quantity names. Scientific qualification can coexist with clearer terminology. |
| Mathematical purpose/conclusion | Finds limits and finite-selection usefulness explained. | Jörn rejects the marked closing distinction as irrelevant to mathematicians and ineffective closure. |
| Other presentation concerns | No matching objections to title, conditional opening, early sampling detail, trivial scaling explanation, correlation explanation/precision, figures/method transitions, or result-first topic sentences. | These human concerns were not surfaced by this audit. Absence is not evidence it actively endorsed every passage. |

This comparison supports retaining the audit for scientific checking while not treating its lack of scientific defects as evidence of human writing acceptance. It does not estimate general detection accuracy, and no acceptance verdict was supplied by that audit.

## UX observations and measurement limits

Copy-paste handoff succeeded on this actual served page. All 12 comments and their anchors were recoverable, including private annotations. This batch contains comments, not bare-highlight-only entries, so bare-mark handoff has not been tested empirically here.

First annotation creation: 10:24:01.378536 UTC. Last annotation creation: 10:37:50.585083 UTC. Export: 10:38:39.849 UTC. First saved annotation to export: about 14 min 38 s; last annotation to export: about 49 s. These intervals include unknown reading, typing, pauses and export work and exclude unknown initial reading/setup time. They are not total active Jörn-time. No screen recording was made available. Ergonomic feedback received below.

## Research consequence

Completed anchored feedback is now available for source-aware revision and for comparing this narrow audit with human concerns. Preserve qualifications and withdrawals. The explicit overall judgment now rules out routing this exact candidate as accepted. The desk does not rewrite the production manuscript; root owns subsequent workflow research.

## Human UX follow-up

Jörn subsequently wrote, verbatim:

```text
i was not interrupted (much) and so this is about thetime i needed to read + write. i think 80% was reading, 15% was typing 5% was selecting text + press "a" + press "ctrl+enter" to save + click close on the sidebar to show the full page again. maybe the last 30s where figuirng out how to export [which i now know]
```

Desk interpretation: the observed roughly 14½-minute window is a reasonable approximate reading/commenting duration according to Jörn, despite unknown time before the first annotation. His subjective allocation is about 80% reading (~12 minutes), 15% typing (~2 minutes), and 5% annotation mechanics (~45 seconds). These rounded estimates are not instrumented measurements or mutually verified durations. About 30 seconds may have been learning export, now familiar. The recurrent UI actions include selection, A, Ctrl+Enter and closing the sidebar to restore page width. This supports retaining Hypothesis for the next item rather than building a different interface now. It does not prove optimality; the prose verdict was supplied separately below.

## Human overall verdict follow-up

Asked what overall verdict to record for this exact sample, Jörn answered verbatim:

> Needs revision before acceptable

Scope: the reviewed contemporary DS subsection, not other candidates or the whole thesis. This establishes non-acceptance of this version while preserving the local praise, conditional judgments and withdrawal recorded above.

## API retrieval trial

After Jörn supplied a personal Hypothesis token through a hidden terminal prompt, an authenticated read-only document query returned all 12 annotations. The API snapshot is retained in `20260918-root-contemporary-ds-reader.api.json`. IDs, comments, targets/selectors, document URIs, creation/update timestamps, tags, permissions and users exactly match the pasted export. No annotations were missing or added. The token was read from `~/.config/hypothesis/api-token`, never included in the response or repository, and each use was disclosed in chat.

A separate query for the PDF trial returned one comment, “test”, with its selected passage. That passage matches the frozen c002 extracted text. This is interface-test data, not a human judgment of prose. Jörn had already confirmed opening the served PDF and annotating worked. Together these establish PDF opening/annotation and API retrieval on his current device, not a test on every browser/device.

Working handoff: Jörn finishes annotating and says done; the agent fetches annotations for the reviewed document using his token. JSON copy-paste remains a fallback.
