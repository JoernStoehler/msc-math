# Endpoint Residualized Regression

Status: no current retained-dataset evidence packet.

Dataset path: `../../dataset/` from this method folder.

Current command: none. A current rerun first needs a clean row-specific script.

The old residualized-regression disposition and report were pre-LICCA source
material only. They were deleted from the active tree after migrating the
current fact: this row needs a current retained-dataset rerun or a terminal
abandoned/not-applicable state before thesis use.

Current method-table role:

- endpoint-only residualized regression beyond metadata;
- supporting/caveat evidence only unless it produces a valid candidate-proposer;
- validity guard is grouped endpoint CV after metadata-first residualization.

Next action:

- write a clean script only if this row remains required after the current
  method inventory.
