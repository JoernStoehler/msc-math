# DS revision reconciled with later human feedback

[Candidate copy](revised.md), [exact diff](revision.diff), [relocated methods details](methods-notes.md), [input identities](input-hashes.json).

This is a surgical revision of the existing ridge-area subsection, not a replacement DS chapter or a PASS claim. The source and human responses remain unchanged in their owning worktrees. No human review, experiment, capacity call, or broad new authoring pass was requested. The supplied source already addresses many original annotations; later microtests expose four surviving issues.

## Applied edit ledger

| Evidence | Surviving source wording | Action and evidence boundary |
|---|---|---|
| Microbatch 2, F | `with seed 42` | Remove from reader prose; retain in methods notes. Jörn specifically objects to the seed's relevance. Do not infer rejection of support-height range or generation conditions. |
| Microbatch 1, C | Prespecified sign criterion; `not a significance test` | Move both sentences to methods notes. Report actual mean difference and eight positive pairs as before. No claim of statistical significance is added. Human found the criterion interruption hard to read and its motivation and denial unhelpful. |
| Microbatch 2, J | Fixed hash order, both geometric selection sets, control footnote | Replace with a short description of same-generator unselected controls chosen without capacities; preserve exact exclusions and deterministic ordering in linked methods notes. Do not relabel controls random, imply unrestricted population representativeness, or erase the second-selector exclusion. |
| Microbatch 2, K | `designed product paths`; `every retained step` | Use two rotation families and four sampled positions in each. Packet README verifies the count and rotation-path interpretation. Retain numerical scope and existing explanatory consequence; do not promote to a symbolic theorem for whole paths. |

## Retained judgments and unresolved items

- **B, full-context follow-up:** the correlation paragraph was explicitly judged “great text of PASSing quality and high value wrt what content it presents!” It is preserved byte-for-byte. The initial snippet concern is not a reason to rewrite it.
- **L:** preserve the explanatory sentence about separate absolute face areas. Jörn said keep. Do not use the scaling objection as a rule against all formula explanations.
- **O:** historical provenance is an upstream evidence problem. Preserve the current limitation until the separate revalidation produces checked replacement evidence. The figure caption's matching caveat therefore remains too. This paragraph is a pending integration item, not an accepted final solution.
- **Original opening:** rejection was conditional on preceding context and then qualified positively. Current opening differs; neither this opening nor the whole revision has human approval.
- **Ordering:** Jörn withdrew pentagon-first reordering criticism. Preserve general edge formula followed by the pentagon specialization.
- **Original 12 annotations:** the source already adds subsection boundaries and figures, removes trivial scaling and ambiguous “targets,” reduces digits and explains correlation, removes the table-maximum aside, and makes the non-universal-ascent observation explicit. These are completed edits, not proof of their adequacy.
- **M/N:** unused `factorial-both` and retrospective/prospective “descriptor” qualification belong to an alternate draft; they do not occur here. No imported removal is needed.
- **Proof microtests P/Q/R/S:** they do not authorize a general ban on qualifications. No mathematical qualification was removed on that theory. The corrected trial recommends human-triaged candidate flaggers; it supplies no acceptance gate.
- **Closing:** the last sentence still contrasts family-specific mathematical identities and population selection. It is more concrete than the originally rejected selection-versus-local-refinement closing, but may still feel repetitive. This is an agent concern, not an exact human rejection of this revised wording; left unchanged.
- **Title:** names the quantities but is less result-oriented than “An inverse association…”; whether to strengthen it is a design choice, not settled by the exact later feedback. Left unchanged.
- **Display:** 18-panel figure may need a full page or a different layout. Image is copied unchanged to keep this packet self-contained. No PDF layout acceptance is claimed.

## Checks

The exact diff contains only the four edits above and the methods-note reference. Formula display blocks, numerical table and correlation paragraph are identical to the supplied revision. Local Markdown image and methods-note targets resolve. The figure bytes match their source. Input hashes freeze the actual feedback read, including its qualifications and withdrawals. No data values were recalculated.

The root can adopt these edits independently of pending historical revalidation. It must reconcile that evidence and resolve methods/figure references at full-chapter integration; this copy should not overwrite unrelated whole-chapter work.
