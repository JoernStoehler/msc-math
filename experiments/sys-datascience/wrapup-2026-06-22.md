# Sys-Datascience Wrapup 2026-06-22

Status: handoff note for reassignment. Scope is the random/product-only
`sys-datascience` slice. Do not infer ascent, continuation, local-maximum,
attractor, or basin claims from this work.

## Merge State

Active branch: `sys-ds-feature-closure`.

Mergeable checkpoint contents:

- scoped random/product prepare tiers via
  `prepare/build-random-only-slice.sh`;
- full scoped random/product rerun artifacts for the main method packets;
- method/status docs updated to say the full rerun is complete and review is
  still pending;
- body-centroid helper spike for translation canonization, with tests, but not
  yet wired into prepared feature columns.

## Current Evidence

Full scoped prepare run used `/tmp/sys-ds-random-only-full`:

- `4096` `random_sample` rows;
- `10240` `random_product_sample` rows;
- `0` ascent rows;
- `0` computed-observation rows;
- max `sys = 0.86258589584944`;
- `0` rows with `sys > 1`;
- full prepare table build time about `409s`, with low memory and CPU-heavy
  geometry/face feature construction.

Current method artifacts support only retained-table random/product evidence:
no positive row, EDA/association/projection/prediction in-table structure, and
no validated generated-candidate proposer.

## Canonization State

Scale: volume-one normalization exists.

Translation:

- `euclidean-polytopes::volume_and_centroid_from_incidence_f64` computes body
  volume plus body centroid from the existing known-incidence origin-star
  decomposition.
- `prepare/canonize.rs` computes volume-one body-centroid translation and
  validates the normalized-dual formula
  `a_i' = a_i / (1 - <a_i,c>)`.
- The helper is tested on simplex and shifted-box fixtures.
- It is not yet exposed as `canon_body_centroid_*` feature columns and has not
  been smoke-checked across retained random/product rows.

`Sp(4)`: no global canonization. Prefer invariant feature families and honest
labels over heuristic gauge fixing.

Facet permutations: most scalar summaries are permutation-invariant; raw arrays
and `poly_id` are not geometric canonical data.

## Remaining Work

High value next steps:

1. Wire body-centroid translated features into prepare under explicit
   `canon_body_centroid_*` names.
2. Smoke-check denominator margins on random/product rows before full rerun.
3. Add principal `4x4` Pfaffian/symplectic-volume minor summaries or decide
   that the existing `sp4-feature-spike` changes are the right starting point.
4. Exclude or relabel coordinate-wise standard deviations from clean
   geometry-only models; they are Euclidean-coordinate diagnostics.
5. Run method/statistics review after the canonized/invariant feature rerun.

Do not spend time tomorrow debating whether vertex-average canonization is the
answer. It is only a diagnostic, not the clean translation canonization.

## Side Worktrees

Keep short-term:

- `.worktrees/sys-ds-method-review`: contains method-review fixes for
  non-gradient perturbation overclaim, prediction/projection feature caps, and
  richer prediction/projection artifacts. Port or explicitly drop.
- `.worktrees/sp4-feature-spike`: contains omega/two-face summary additions.
  Port or explicitly drop after comparing against the planned Pfaffian/minor
  feature work.

Deleted during wrapup:

- `.worktrees/sys-translation-canonization-spike`;
- `.worktrees/random-only-sys-datascience`.

Saved scratch from deleted worktrees:

- `/tmp/sys-ds-worktree-cleanup/translation-canonization-spike.md`;
- `/tmp/sys-ds-worktree-cleanup/random-parameter-sensitivity/`;
- dirty diffs/status files for both deleted worktrees.

The old `random-parameter-sensitivity` packet was not present in the active
branch. It may be useful later, but its main questions overlap with current
`random-tail-eda` filtered source/parameter summaries.

## Checks

Centroid/helper checks run during wrapup:

- `rustfmt --check` on touched Rust files;
- `cargo test -p euclidean-polytopes --test volume`;
- `cargo test -p exp-sys-landscape canonize --bins`.
