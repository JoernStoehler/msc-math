% math.tex — \input'd from thesis/experiments.tex
%
% Status: Phase 1 complete. 29 UNKNOWNs found in Lagrangian products (all f64 rounding noise, gaps 4.44e-16 to 4.93e-12).
%
% Sources: crates/dev-numerical-analysis/unknown-predicates/unknown-predicates.jsonl
%          crates/dev-numerical-analysis/unknown-predicates/unknown_predicates_beta_min.png
%          crates/dev-numerical-analysis/unknown-predicates/logbook.md

\subsection{Numerical Precision of the Admissibility Check}
\label{sec:unknown-predicates}

The capacity algorithm uses a three-valued admissibility predicate for each
candidate orbit $(S, \sigma)$: the KKT solution $\beta$ is classified as
\emph{certified} ($\beta_i > \varepsilon$ for all~$i$),
\emph{rejected} ($\beta_i < -\varepsilon$ for some~$i$), or
\emph{uncertain} ($-\varepsilon < \beta_{\min} \le \varepsilon$),
where $\varepsilon = 10^{-12}$.
The certified capacity uses only certified orbits; the uncertain capacity
additionally admits uncertain orbits.
A gap between the two (numerical gap $> 0$) would indicate that an
uncertain orbit achieves lower action than any certified orbit, meaning
the algorithm's output depends on floating-point precision.

\paragraph{Experiment.}
We re-ran both the random-sweep dataset (70~polytopes, $F = 5, \ldots, 12$)
and the Lagrangian-products dataset (92~polytopes: pentagon $5 \times 5$ sweep
and 10 regular polygon pairs) through the production algorithms
(\texttt{ehz\_capacity} and \texttt{billiard\_capacity} respectively),
recording the certified capacity, uncertain capacity, numerical gap,
and minimum $\beta$ component for each polytope.

\paragraph{Results.}
Of the 162~polytopes, 29 exhibited a nonzero numerical gap, all from the
Lagrangian-products dataset; the random-sweep dataset produced zero
UNKNOWN predicates.
The 29 nonzero gaps range from $4.4 \times 10^{-16}$ to $4.9 \times 10^{-12}$,
all below $10^{-10}$, and are attributable to \texttt{f64} rounding in the
billiard capacity computation rather than genuine numerical ambiguity.
In all 29 cases the certified and uncertain capacities agree to full
\texttt{f64} precision up to these rounding errors.

The minimum $\beta$ component $\beta_{\min}$ of the certified orbit measures
the margin from the uncertain threshold.
For random polytopes, $\beta_{\min}$ ranged from $6.7 \times 10^{-4}$
to $1.2 \times 10^{-1}$ (median $4.5 \times 10^{-2}$), well above the
threshold $\varepsilon = 10^{-12}$.
For Lagrangian products, $\beta_{\min}$ ranged from $5.5 \times 10^{-2}$
to $3.5 \times 10^{-1}$ (median $1.7 \times 10^{-1}$), likewise well above
the threshold.

\paragraph{Conclusion.}
The algorithm is empirically exact up to machine-precision rounding at
\texttt{f64} on our datasets: no UNKNOWN predicates influenced the capacity
computation in any meaningful way.
The gaps observed in Lagrangian products are consistent with \texttt{f64}
arithmetic noise ($\lesssim 10^{-12}$) rather than structural precision
failures.
Phase~2 (high-precision re-solve) is not required on the current datasets,
though confirming that the 29 observed UNKNOWN cases are truly benign would
require verifying each gap individually.
