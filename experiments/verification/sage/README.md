# Sage Validation

This directory is the durable home for reusable Sage comparison and validation
surfaces.

Use this area when a Sage implementation is no longer topic-local and instead
checks a reusable algorithm contract that matters across experiments.

Keep topic-specific exploratory Sage scripts next to the producer until they
stabilize enough to promote here.
