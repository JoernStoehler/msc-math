# Table-Column Regression

Status: no current retained-dataset evidence packet.

Dataset path: `../../dataset/` from this method folder.

Current command: none. A current rerun first needs a clean row-specific script.

The old `feature-pattern-search/` regression artifacts were deleted instead of
rescued because they mixed several method rows, stale pre-LICCA reports,
duplicate local `feature_*.jsonl` views, and shared helper code inside a
pseudo-method folder.

Current method-table role:

- test whether retained table-column groups trained on random rows predict
  endpoint rows;
- the load-bearing guard is random-to-endpoint prediction under grouped split;
- a useful result is either a candidate-proposer or a bounded no-search-output
  report from the current retained dataset.

Next action:

- write a clean script only if this row remains required after the current
  method inventory; do not resurrect old `feature-pattern-search` code unless
  a reviewer finds positive expected value after contamination risk.
