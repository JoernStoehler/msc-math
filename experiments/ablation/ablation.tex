% ablation.tex — \input'd from thesis appendix (Part 4, to be written)
%
% Status: Phase A+B complete. Four variants: V0 (unpruned), V1 (undirected
%   adjacency pruning), V4 (directed Reeb-flow adjacency), V5 (SVD-only solver).
%
% Sources: experiments/ablation/ablation.jsonl (generated by ablation.rs),
%   experiments/ablation/ablation_timing.png (generated by ablation.py).
%
% Mathematical references used here (all Jörn-approved in commit 0839595):
%   lem:adjacency-constraint — consecutive facets with positive dwell time are adjacent
%   cor:adjacency-pruning — discard (S,σ) with non-adjacent consecutive pairs

\subsection{Algorithm Variant Ablation}
\label{sec:ablation-pruning}

We compare four variants of the HK2017 capacity algorithm on a fixed dataset
of 38 polytopes. The goal is to verify that each pruning strategy and solver
variant preserves correctness, and to measure the resulting speedup.

\paragraph{Variants.}
Table~\ref{tab:ablation-variants} lists the four algorithm variants.
V0 is the unpruned reference.
V1 uses undirected vertex adjacency (Corollary~\ref{cor:adjacency-pruning}).
V4 strengthens V1 with a directed symplectic condition on the adjacency graph.
V5 replaces the LU+SVD solver with SVD-only to test the LU fast path's overhead.

\begin{table}[h]
\centering
\begin{tabular}{lp{9cm}}
\toprule
Variant & Description \\
\midrule
V0 & Unpruned HK2017: exhaustive search over all subsets~$S$ and all cyclic orderings~$\sigma$ of~$S$ \\
V1 & Undirected adjacency pruning: restricts to orderings~$\sigma$ that form a Hamiltonian cycle in the undirected facet graph~$G_K[S]$ (Corollary~\ref{cor:adjacency-pruning}) \\
V4 & Directed Reeb-flow pruning: restricts to orderings~$\sigma$ forming a Hamiltonian cycle in a \emph{directed} graph where $i \to j$ requires both vertex adjacency and $\omega_0(n_i, n_j) \le 0$ \\
V5 & SVD-only solver: same pruning as V1, but skips the LU fast path and uses SVD with gap-based rank detection directly \\
\bottomrule
\end{tabular}
\caption{Algorithm variants compared. V0 is the most trusted baseline
  (fewest assumptions, simplest code path). V1 is the production algorithm.
  V4 and V5 are experimental variants testing stronger pruning and solver alternatives.}
\label{tab:ablation-variants}
\end{table}

\paragraph{Directed adjacency (V4).}
The HK2017 algorithm's $Q$-function encodes the cyclic orbit ordering such that
for the $Q$-maximizing permutation, consecutive transitions $\sigma(k) \to \sigma(k{+}1)$
satisfy $\omega_0(n_{\sigma(k)}, n_{\sigma(k+1)}) \le 0$.
This follows from the sign convention in $Q(\beta) = \sum_{i>j} \beta_i \beta_j\, \omega_0(n_{\sigma(i)}, n_{\sigma(j)})$:
the algebraic ordering is the reverse of the physical Reeb orbit direction.
V4 exploits this by building a directed graph where edge $i \to j$ is allowed only when
$\omega_0(n_i, n_j) \le \varepsilon$ (with $\varepsilon = 10^{-8}$ for numerical tolerance).
% [TODO: JÖRN - verify the sign analysis. Agent derived that Q-maximizing
%  permutations have ω₀ ≤ 0 for consecutive pairs, confirmed empirically on
%  all 38 test polytopes. But the derivation needs mathematical verification.]

\paragraph{Test polytopes.}
Table~\ref{tab:ablation-dataset} describes the 38 test polytopes.
Random polytopes use a fixed seed (42) and height range $h \in [0.5, 2.0]$.

\begin{table}[h]
\centering
\begin{tabular}{llrp{5.5cm}}
\toprule
Group & $F$ & Count & Description \\
\midrule
Random generic    & 5--8 & 20 & 5 random 4-polytopes per facet count \\
Random Lagrangian & 6--8 & 15 & 5 random products per pair:
  $\triangle{\times}\triangle$,
  $\triangle{\times}\square$,
  $\square{\times}\square$ \\
Regression cases  & 7--8 &  3 & Fixed polytopes triggering degenerate KKT systems \\
\bottomrule
\end{tabular}
\caption{Test polytope dataset. All random polytopes use seed~42, $h \in [0.5, 2.0]$.
  Regression cases: $\triangle{\times}\square$ at $\theta{=}0^\circ$ and
  $\square{\times}\square$ at $\theta{=}0^\circ$ trigger rank-deficient KKT systems;
  the hypercube exercises the LU fast path.}
\label{tab:ablation-dataset}
\end{table}

\paragraph{Correctness.}
All four variants return identical capacities on all 38 test polytopes (maximum
absolute difference $< 10^{-8}$).
This confirms that neither adjacency pruning (V1), directed pruning (V4), nor
the SVD-only solver (V5) discards the optimal orbit on this dataset.

The three regression cases additionally verify correct handling of degenerate
KKT systems:
$\triangle{\times}\square$ at $\theta{=}0^\circ$ (expected $3\sqrt{2}/2 \approx 2.121$) and
$\square{\times}\square$ at $\theta{=}0^\circ$ (expected $2.0$) both require a null-space search
when the LU factorization detects rank deficiency;
the hypercube (expected $4.0$) is solved by the LU fast path without SVD.
All three capacities match their expected values to within $10^{-6}$.

\paragraph{Speedup.}
Table~\ref{tab:ablation-speedup} shows mean computation times.
V1's undirected pruning gives ${\sim}3\times$ speedup at $F = 8$.
V4's directed pruning gives a dramatic additional improvement:
${\sim}140\times$ over V0 for generic polytopes at $F = 8$, and
${\sim}40\times$ for Lagrangian products (whose structured normals yield
more zero-valued $\omega_0$ pairs, limiting directed pruning effectiveness).
V5 (SVD-only) is slightly faster than V1, consistent with the LU fast path
adding marginal overhead when most permutations yield infeasible~$\beta$.
Standard deviations are shown in Figure~\ref{fig:ablation-timing}.

\begin{table}[h]
\centering
\begin{tabular}{llrrrr}
\toprule
Group & $F$ & V0 (ms) & V1 (ms) & V4 (ms) & V5 (ms) \\
\midrule
Generic    & 5 &   0.5 &   0.5 &  0.1 &   0.6 \\
           & 6 &   3.4 &   2.7 &  0.2 &   2.8 \\
           & 7 &  28.2 &  14.7 &  0.6 &  13.3 \\
           & 8 & 250.4 &  78.4 &  1.8 &  67.7 \\
\midrule
Lagrangian & 6 &   3.2 &   3.2 &  0.7 &   3.5 \\
           & 7 &  29.3 &  15.0 &  2.0 &  13.1 \\
           & 8 & 253.8 &  84.7 &  6.6 &  71.0 \\
\bottomrule
\end{tabular}
\caption{Mean computation time (ms) for all four variants,
  $n = 5$ samples per group per facet count.
  V4 achieves ${\sim}140\times$ speedup over V0 at $F = 8$ (generic)
  due to the directed $\omega_0$ condition pruning ${\sim}99\%$ of
  candidate orderings.}
\label{tab:ablation-speedup}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{../experiments/ablation/ablation_timing.png}
\caption{Timing comparison of all four algorithm variants,
  mean $\pm$ std ($n=5$).
  Left: random generic polytopes. Right: random Lagrangian products.
  V4~(directed, green squares) achieves the strongest pruning, with
  speedups growing from ${\sim}5\times$ at $F = 5$ to ${\sim}140\times$ at $F = 8$
  for generic polytopes.}
\label{fig:ablation-timing}
\end{figure}
