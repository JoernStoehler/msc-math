# Stage 1 output — large, regenerate with collect_poly
collected_poly.jsonl
collected_synth.jsonl

# Ad-hoc filter/results outputs (not committed — use testdata/ for stable test cases)
filtered_*.jsonl
results_*.jsonl
