# HKO Reference And Local Geometry

Use / maintenance model: seed-level topic map for HKO-related questions that
touch sys-datascience. Keep HKO-local experiment evidence linked to its owning
experiment folder or a reviewed packet. Update this file when HKO evidence
changes how other datascience topics should reason.

Scope: relation between the known HKO example/reference row, local HKO
perturbations, ridge-area features, and random/product generated data.

Current belief: HKO is useful as a reference point, but current HKO/ridge work
inside sys-datascience is smoke-scale or reference-coverage evidence. The
HKO-local relation should be a separate topic, not folded into scalar-proposer
cleanup.

Owner-readiness/status: topic seed; parked unless a mechanism or HKO-local
session explicitly reopens it. Do not route HKO-local maximality claims through
sys-datascience coordination.

Evidence sources:

- `../../methods/hko-reference-coverage/`
- `../../methods/hko-ridge-source-smoke/`
- `../../methods/tail-survival-1m-posterior/`, only for tail/scale-up context;
  it intentionally excludes the earlier tainted HKO-distance/flank section.

Adjacent topics to read:

- `geometric-feature-mechanisms.md`, for ridge-area mechanism hypotheses.
- `generated-candidate-proposers.md`, for random/product ridge-filter evidence.
- `tail-rare-event-modeling.md`, for rare-event scale-up caveats.

Candidate hypotheses:

- HKO is locally high in `sys` and locally extremal in some ridge-area
  magnitude summaries under a corrected HKO-local perturbation setup.
- The ridge-area relation seen in random/product data and an HKO-local relation
  may share a mechanism.
- HKO sits in or near current random-product `5x5` support, but naive random
  products rarely approach the relevant structured region.
- Prepared-table normalization can make naive HKO-distance metrics invalid
  unless the scale convention is handled explicitly.

Cheap discriminators:

- Review the current source-reproducible HKO ridge smoke packet and decide
  whether it should become a non-smoke HKO-local packet.
- Define a scale-invariant or correctly normalized HKO-distance metric before
  using retained product rows for HKO flank claims.
- Compare HKO reference feature ranks against bucket-matched random/product
  distributions.

Ready packet prompts:

- HKO ridge-source smoke interpretation review. `reviewer-ready`.
  Objective: decide exactly what
  `../../methods/hko-ridge-source-smoke/` supports and does not support for
  sys-datascience mechanism claims. Output should distinguish plumbing,
  smoke-scale evidence, HKO reference evidence, and HKO-local evidence.

Needs topic-owner sharpening:

- Corrected HKO-distance metric packet.
- HKO reference rank report for current invariant features.
- Non-smoke HKO perturbation/ridge-area packet, owned by the HKO-local slice if
  it becomes about local maximality rather than sys-datascience reference
  evidence.

Opportunity-cost notes: this is potentially thesis-interesting, but it should
not block data-science method-surface closure unless it changes wording about
the meaning of ridge-area features.
