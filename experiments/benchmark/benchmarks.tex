% benchmarks.tex — \input'd from experiments/experiments.tex
%
% Sources: experiments/benchmark/benchmark.jsonl (self-contained timing dataset, generated by benchmark.rs)
%
% Structure:
%   - Timing statistics table (HK2017 pruned by facet count)
%   - Exponential growth model
%   - Pruned vs unpruned comparison table
%   - Billiard vs HK2017 comparison table
%   - Algorithm selection summary
%   - Timing figure with fitted models

\subsection{Benchmarks and Algorithm Selection}
\label{sec:benchmark}

We benchmark the HK2017 and billiard algorithms on random polytopes
($F = 5, \ldots, 12$) and Lagrangian products to establish practical
performance limits and guide algorithm selection. Table~\ref{tab:benchmark-stats}
shows timing statistics for HK2017 pruned on 83 random polytopes.

\begin{table}[h]
\centering
\begin{tabular}{rrrrrr}
\toprule
$F$ & $N$ & Median (ms) & Mean (ms) & Min (ms) & Max (ms) \\
\midrule
 5 & 10 &    0.043 &    0.042 &    0.031 &    0.055 \\
 6 & 10 &    0.234 &    0.223 &    0.149 &    0.265 \\
 7 & 10 &    0.396 &    0.418 &    0.190 &    0.631 \\
 8 & 15 &    1.098 &    1.172 &    0.593 &    1.860 \\
 9 & 15 &    4.722 &    5.004 &    3.172 &    7.588 \\
10 & 15 &   23.910 &   25.086 &   19.578 &   29.825 \\
11 &  5 &  174.464 &  168.231 &  153.235 &  175.831 \\
12 &  3 & 1{,}755.8 & 1{,}629.2 & 1{,}325.4 & 1{,}806.3 \\
\bottomrule
\end{tabular}
\caption{HK2017 pruned computation time for random 4-polytopes by facet count.}
\label{tab:benchmark-stats}
\end{table}

A log-linear regression on the median times yields
\[
  T_{\mathrm{pruned}}(F) \;=\; 2.1 \times 10^{-8} \cdot 4.2^F \quad\text{seconds}
  \qquad (R^2 = 0.970),
\]
with growth rate~${\sim}4\times$ per facet. Extrapolating to the
4D crosspolytope ($F = 16$), the model predicts ${\sim}222$~seconds
(3.7~minutes) per capacity computation.

For $F \le 7$, we compare pruned vs unpruned on random polytopes
(Table~\ref{tab:pruned-vs-unpruned}). On Lagrangian products, the billiard
algorithm is consistently 2--7$\times$ \emph{slower} than HK2017 pruned
(Table~\ref{tab:billiard-vs-hk2017}); we use it regardless because its
polynomial-time guarantee matters for large Lagrangian products.

\begin{table}[h]
\centering
\begin{tabular}{rrrr}
\toprule
$F$ & Pruned (ms) & Unpruned (ms) & Speedup \\
\midrule
 5 &  0.043 &  0.371 &  8.6$\times$ \\
 6 &  0.234 &  4.091 & 17.5$\times$ \\
 7 &  0.396 & 16.814 & 42.5$\times$ \\
\bottomrule
\end{tabular}
\caption{Pruned vs unpruned timing for small random polytopes.}
\label{tab:pruned-vs-unpruned}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{rrrr}
\toprule
$F$ & Billiard (ms) & HK2017 pruned (ms) & Slowdown \\
\midrule
 6 &   1.110 &  0.472 &  2.4$\times$ \\
 7 &   4.964 &  1.279 &  3.9$\times$ \\
 8 &  22.273 &  3.990 &  5.6$\times$ \\
 9 & 116.014 & 17.461 &  6.6$\times$ \\
10 & 529.476 & 78.276 &  6.8$\times$ \\
\bottomrule
\end{tabular}
\caption{Billiard vs HK2017 pruned on random Lagrangian products (billiard slowdown factor shown).}
\label{tab:billiard-vs-hk2017}
\end{table}

Based on these benchmarks, we adopt the following algorithm selection:

\begin{itemize}
  \item \textbf{Lagrangian products}: Use billiard (polynomial cost).\\
    Rust: \texttt{billiard\_capacity}.
  \item \textbf{General polytopes}: Use HK2017 pruned.
    Practical limit: $F \le 12$ for routine datasets,
    $F \le 14$ for one-off computations.
    Rust: \texttt{ehz\_\allowbreak capacity}.
  \item \textbf{Debug testing}: Use HK2017 unpruned on $F \le 7$ to exercise
    all code paths with internal assertions.
    Rust: \texttt{ehz\_\allowbreak capacity\_\allowbreak unpruned}.
\end{itemize}

Figure~\ref{fig:benchmark-timing} shows all timing data with fitted exponential
models for each algorithm and polytope class.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{../experiments/benchmark/benchmark_timing.png}
\caption{EHZ capacity benchmarks. Each algorithm and polytope class shows
  individual samples and a fitted exponential model. HK2017 pruned on random
  polytopes (fitted model extrapolated to crosspolytope, $F = 16$) shows the
  steepest growth. Billiard on Lagrangian products is consistently slower than
  HK2017 pruned on the same polytope class, but is used for its polynomial-time guarantee.}
\label{fig:benchmark-timing}
\end{figure}
