# Fresh HKO exposition in progress

Current assembled candidate: `chapter-preview.tex` / `chapter-preview.pdf`
(seven pages, September 14). It combines the fresh opening with all recovered
mathematical inputs and a newly written `chart-completion.tex`. The latter
proves the Hausdorff-to-labelled-coordinate reduction, excludes new vertices
including singular nonface tuples by a bounded subsequence argument, and
justifies smooth volume and the symmetry action. A targeted Luna inspection
found no material gap in these interfaces; this is not an independent theorem
certificate rerun or Jörn acceptance. The coordinating agent added the general
closure matrix and explicit upper-function definitions after its own reading.

`sections-readable.tex` replaces all 20 terminating integer fractions in the
input table with mathematically identical finite decimals. Conversion checked
integer equality for each fraction; no rounding occurred. The PDF states the
exact-decimal convention. The original input table is unchanged. The resulting
table was rendered and visually inspected; the selected build log is clean.

Remaining production work: integrate chapter references/citations into the
whole manuscript, improve transitions from the prerequisite capacity chapter,
and incorporate human reading feedback. This assembled candidate supersedes
the incompleteness description below, which records the earlier opening alone.

`proof-opening.tex` is a fresh explanatory opening, built as `opening-preview.pdf`. It derives the uniform decrease from finitely many touching upper bounds and explains the symmetry slice. `opening-review.md` records the bounded mathematical review and resolved findings. This is not a complete chapter or an independent certification of the finite theorem packet.

Mathematical inputs: `../hko-writing-input/`, including the ten dual rows, full-chart volume derivative, a worked seven-facet section, all 26 exact section choices, row formulas and exact acceptance predicates. Formal implication source: `formal/hko-feasible-section-upper-branches.tex` from the repository root. Prose is composed afresh.

The completed chapter still needs the concrete geometry and calculations, the finite verification result with its retained status, and the promised Hausdorff-to-chart reduction. That reduction uses continuity of polarity in an origin-interior neighborhood, exposed neighborhoods that label the ten polar vertices, and open determinant/slack conditions for the simple labelled representation. Merely asserting a fixed chart does not prove the larger theorem domain.

The separate data-science reading sample is fixed pending Jörn's feedback; this draft does not replace that review target.
