% math.tex — \input'd from thesis/experiments.tex
%
% Sensitivity analysis and gradient-based improvement of the systolic ratio.
%
% Sources:
%   - Generated data: experiments/sys-optimization/sys-optimization-sensitivity.jsonl
%   - Generated data: experiments/sys-optimization/sys-optimization-steps.jsonl
%   - Generated data: experiments/sys-optimization/sys-optimization-iterations.jsonl
%   - Generated data: experiments/sys-optimization/sys-optimization-validity.jsonl
%   - Plot script: experiments/sys-optimization/analyze.py
%   - Developer notes: experiments/sys-optimization/logbook.md
%
% Structure:
%   - Motivation and approach
%   - Derivative computation (formal lemmas for dual vertices a_k)
%   - Step bound algorithm
%   - Sensitivity analysis (Phase 1)
%   - Finite gradient steps (Phase 2)
%   - Iterative gradient ascent (Phase 3)
%   - Gradient validity testing (Phase 4)
%   - Discussion

\subsection{Sensitivity Analysis and Gradient Optimization of sys}
\label{sec:sys-optimization}

Random sampling has not found $\mathrm{sys} > 1$ (see
Sections~\ref{sec:random-sweep} and~\ref{sec:random-product-sweep}).
Instead of relying on random exploration, we compute the gradient
$\nabla_a\,\mathrm{sys}$ of the systolic ratio with respect to
the dual vertices $a = (a_1,\dots,a_F) \in (\mathbb{R}^4)^F$,
and take directed steps to increase $\mathrm{sys}$.

\subsubsection*{Method}

For a polytope $K = \{x \in \mathbb{R}^4 \mid \langle a_k, x \rangle \le 1,\; k=1,\dots,F\}$
with dual vertices $a_k \in \mathbb{R}^4 \setminus \{0\}$,
the systolic ratio decomposes as
$\mathrm{sys} = c_{\mathrm{EHZ}}^2 / (2\,\mathrm{vol})$.
We compute the sensitivity of each factor to changes in the dual vertices.
The gradient $\partial/\partial a_k \in \mathbb{R}^4$ is a single vector
per facet, without the manifold constraints that arise in the unit-normal
parameterization.

All quantities needed for the gradient ($\beta^*$, $\mu$, $\xi$, $Q^*$,
facet volumes $S_k$, facet centroids $\bar{x}_k$) are either already computed
by the HK2017 algorithm's KKT solve or can be extracted from the polytope data
at negligible additional cost.

We first state the general principle, then apply it to volume and capacity
derivatives in turn.

\begin{theorem*}[Envelope theorem for constrained optimization]
Let $f(x;\theta)$ be a smooth objective and $g_i(x;\theta) = 0$,
$i = 1,\dots,m$, smooth equality constraints, with parameter $\theta$.
Let $x^*(\theta)$ be a non-degenerate KKT point with multipliers
$\lambda^*(\theta)$, and let
$\mathcal{L}(x,\lambda;\theta) = f(x;\theta) - \sum_i \lambda_i g_i(x;\theta)$
be the Lagrangian.
Then the optimal value $f^*(\theta) = f(x^*(\theta);\theta)$ satisfies
\[
  \frac{\partial f^*}{\partial \theta}
  = \frac{\partial \mathcal{L}}{\partial \theta}\bigg|_{x=x^*,\,\lambda=\lambda^*}
  = \frac{\partial f}{\partial \theta}\bigg|_{x^*}
    - \sum_i \lambda_i^* \frac{\partial g_i}{\partial \theta}\bigg|_{x^*}.
\]
\end{theorem*}

\paragraph{Capacity derivative.}

% [TODO: JÖRN - verify statement and proof — replaces old lem:cap-derivative + lem:cap-derivative-normal]
\begin{unverified}
\begin{lemma}[Capacity derivative w.r.t.\ dual vertices]
\label{lem:cap-derivative}
Fix an orbit $(S,\sigma)$ with $|S| = m$ facets. The orbit's action is
$A(S,\sigma;a) = 1/(2\,Q^*)$, where $Q^*$ is the maximum of the KKT problem
\[
  Q(\beta) = \tfrac{1}{2}\beta^T H \beta
  \quad\text{subject to}\quad
  N^T\beta = 0,\; \eta^T\beta = 1,
\]
with $H$ the symmetric matrix from Lemma~\ref{lem:H-quadratic},
$N = [a_{\sigma(1)} | \cdots | a_{\sigma(m)}]$, and
$\eta = \mathbf{1}$ (all ones), so the normalization constraint is
$\sum_i \beta_i = 1$.

Let $\mu \in \mathbb{R}^4$ be the Lagrange multiplier for the closing
constraint $N^T\beta = 0$ at the optimum. If facet $k$ appears in the
orbit at position $i_0$ (i.e.\ $\sigma(i_0) = k$), define
\[
  P_{i_0} = \sum_{i < i_0} \beta_i^*\,a_{\sigma(i)}.
\]
Then
\[
  \frac{\partial Q^*}{\partial a_k}
  = \beta_{i_0}^*\,\bigl[J_0(2\,P_{i_0} + \beta_{i_0}^*\,a_k) + \mu\bigr],
\]
and the capacity derivative is
\[
  \frac{\partial c}{\partial a_k}
  = -\frac{1}{2\,(Q^*)^2}\,\frac{\partial Q^*}{\partial a_k}.
\]
If $k \notin S$, then $\partial c / \partial a_k = 0$.
\end{lemma}

\begin{proof}
The Lagrangian is
$\mathcal{L} = \tfrac{1}{2}\beta^T H\beta + \mu^T N^T\beta + \xi(\mathbf{1}^T\beta - 1)$,
with stationarity condition $H\beta + N\mu + \mathbf{1}\xi = 0$.
Since $\eta = \mathbf{1}$ does not depend on~$a_k$, the dual vertices enter
through $H$ and~$N$ only.
By the envelope theorem, $\partial Q^*/\partial a_k$ involves only the
explicit dependence of $H$ and $N$ on $a_k$:
\[
  \frac{\partial Q^*}{\partial a_k}
  = \frac{1}{2}\frac{\partial((\beta^*)^T H\beta^*)}{\partial a_k}
    + \mu^T\frac{\partial(N^T\beta^*)}{\partial a_k}.
\]
For the $N$-term: only column~$i_0$ of $N$ depends on $a_k$, giving
$\mu^T(\partial N^T\beta^*/\partial a_k) = \beta_{i_0}^*\,\mu$
(as a vector in $\mathbb{R}^4$).

For the $H$-term: $Q = \frac{1}{2}\beta^T H\beta
= \sum_{j < i} \beta_i \beta_j\,\omega_0(a_{\sigma(j)}, a_{\sigma(i)})$
by Lemma~\ref{lem:H-quadratic}. Differentiating the terms involving $a_k$
($\sigma(i_0) = k$) and using the closing constraint
$\sum_i \beta_i^* a_{\sigma(i)} = 0$ to simplify yields
$\frac{1}{2}\partial(\beta^T H\beta)/\partial a_k
= \beta_{i_0}^*\,J_0(2P_{i_0} + \beta_{i_0}^* a_k)$.
(The detailed sign chain involves the antisymmetry of $\omega_0$ and the
bilinear identity $\omega_0(u,v) = (J_0 u) \cdot v$.)

Combining the H-term and N-term:
$\partial Q^*/\partial a_k
= \beta_{i_0}^*\,[J_0(2P_{i_0} + \beta_{i_0}^* a_k) + \mu]$.
Since $c = 1/(2Q^*)$, the chain rule gives
$\partial c/\partial a_k = -(\partial Q^*/\partial a_k) / (2\,(Q^*)^2)$.
\end{proof}
\end{unverified}

\paragraph{Volume derivative.}

% [TODO: JÖRN - verify statement and proof — replaces old lem:vol-derivative + lem:vol-derivative-normal]
\begin{unverified}
\begin{lemma}[Volume derivative w.r.t.\ dual vertices]
\label{lem:vol-derivative}
For a convex polytope $K = \{x : \langle a_k, x \rangle \le 1\}$ with
$0 \in \operatorname{int}(K)$,
\[
  \frac{\partial\,\mathrm{vol}(K)}{\partial a_k}
  = -\frac{S_k}{\|a_k\|^3}\,a_k
    + \frac{1}{\|a_k\|}\,\nabla_{n_k}\mathrm{vol},
\]
where $S_k = \mathrm{vol}_3(F_k)$ is the $3$-dimensional volume of facet
$F_k$, $n_k = a_k/\|a_k\|$ is the unit normal, $h_k = 1/\|a_k\|$ is the
support height, and
$\nabla_{n_k}\mathrm{vol} = -S_k\,(\bar{x}_k - h_k\,n_k)$
is the tangential volume gradient with $\bar{x}_k$ the centroid of~$F_k$.
\end{lemma}

\begin{proof}
The volume depends on $a_k$ through the height $h_k = 1/\|a_k\|$ and
the unit normal $n_k = a_k/\|a_k\|$.  By the chain rule,
\[
  \frac{\partial\,\mathrm{vol}}{\partial a_k}
  = \frac{\partial\,\mathrm{vol}}{\partial h_k}\,
    \frac{\partial h_k}{\partial a_k}
  + \left(\frac{\partial\,\mathrm{vol}}{\partial n_k}\right)^{\!T}
    \frac{\partial n_k}{\partial a_k}.
\]

\emph{Height term.}
Moving facet $k$ outward by $\delta$ enlarges $K$ by a slab of
approximate volume $\delta \cdot S_k$, so
$\partial\,\mathrm{vol}/\partial h_k = S_k$.
Since $h_k = \|a_k\|^{-1}$, we have
$\partial h_k/\partial a_k = -a_k/\|a_k\|^3$.

\emph{Normal term.}
Tilting facet $k$ by $\varepsilon\delta$ (with $\delta \perp n_k$)
replaces the constraint $\langle n_k, x \rangle \le h_k$ with
$\langle n_k + \varepsilon\delta, x \rangle \le h_k$, changing the volume by
$-\varepsilon\,S_k\,(\bar{x}_k \cdot \delta) + O(\varepsilon^2)$.
The tangential gradient (projected to $T_{n_k}S^3$) is
$\nabla_{n_k}\mathrm{vol} = -S_k\,(\bar{x}_k - h_k\,n_k)$.
Since $\partial n_k/\partial a_k = (I - n_k n_k^T)/\|a_k\|$, and
$\nabla_{n_k}\mathrm{vol} \perp n_k$, the projection acts as the identity:
$(\nabla_{n_k}\mathrm{vol})^T (I - n_k n_k^T)/\|a_k\|
= \nabla_{n_k}\mathrm{vol} / \|a_k\|$.

Combining both terms yields the stated formula.
\end{proof}
\end{unverified}

\emph{Derivative cross-checks.}
Both the capacity and volume analytical derivatives are cross-checked against
central finite differences ($\varepsilon = 10^{-6}$) on fixture polytopes
(hypercube, simplex) with relative tolerance~$10^{-4}$.
Agreement validates the implementation against the lemma formulas.
The Phase~4 validity tests further confirm that the gradient accurately
predicts $\Delta\mathrm{sys}$ at small step sizes
(Section~\ref{sec:gradient-validity}).

% [TODO: JÖRN - verify statement and proof sketch — was prop:capacity-piecewise-smooth]
\begin{unverified}
\begin{proposition}[Capacity as piecewise-smooth function of dual vertices]
\label{prop:capacity-piecewise-smooth}
Let $K(a)$ be a \emph{simple} polytope (every vertex determined by exactly~$4$
facets) with variable dual vertices $a = (a_1,\dots,a_F)$.
\begin{enumerate}
\item[\textup{(a)}]
  \emph{Combinatorial type stability.}
  For each $a_0$ such that $K(a_0)$ is a simple polytope, the vertex-facet
  incidence graph is locally constant: there exists $\varepsilon > 0$ such that
  $K(a)$ has the same combinatorial type for all $\|a - a_0\| < \varepsilon$.
  In particular, the set of valid orbit nodes $(S,\sigma)$ surviving
  the HK2017 pruning is constant in this neighborhood.

\item[\textup{(b)}]
  \emph{Per-orbit smoothness.}
  Fix an orbit $(S,\sigma)$ whose KKT problem has a unique non-degenerate
  maximum. Then the action $A(S,\sigma;\cdot)$ is smooth in~$a$,
  with derivatives given by Lemma~\ref{lem:cap-derivative}.

\item[\textup{(c)}]
  \emph{Capacity at generic parameters.}
  The capacity
  \[ c_{\mathrm{EHZ}}(a) = \min_{(S,\sigma)} A(S,\sigma;a) \]
  is continuous.
  At parameters where the minimizing orbit is unique, $c_{\mathrm{EHZ}}$ is
  differentiable with gradient given by that orbit's envelope theorem
  formula. The unique-minimizer case is generic: for any two orbits,
  the equation $A(S_1,\sigma_1) = A(S_2,\sigma_2)$ cuts out a hypersurface
  in the $4F$-dimensional parameter space, so the set of parameters where
  any two orbits tie has measure zero.

\item[\textup{(d)}]
  \emph{Capacity at switching boundaries.}
  When $r \ge 2$ orbits achieve minimum action simultaneously,
  the capacity is non-smooth.
  The directional derivative in direction~$d = (d_{a_1},\dots,d_{a_F})$ is
  \[
    D_d\,c_{\mathrm{EHZ}}
    = \min_{i=1,\dots,r} \nabla_a A(S_i,\sigma_i) \cdot d,
  \]
  i.e.\ the most pessimistic rate of change among the tied orbits.
\end{enumerate}
\end{proposition}

\begin{proof}[Proof sketch]
(a) Each vertex of a simple polytope is the unique solution of a $4 \times 4$
linear system $A_v\,x = \mathbf{1}$ where $A_v$ is the matrix of the four
determining dual vertices. Since $A_v$ is invertible, vertices move
continuously (indeed smoothly) in~$a$.
For each vertex, the strict inequalities
$\langle a_j, v \rangle < 1$ for non-determining facets are open conditions, so they
persist under small perturbations of~$a$.
Similarly, for ridge-adjacent facets, $\omega_0(a_i, a_j) \neq 0$ is an open
condition, so the directed adjacency graph (and hence the pruned orbit set)
is locally constant.
(b) Follows from the envelope theorem applied to the KKT problem, whose
optimum is locally unique and smooth by the implicit function theorem
(given LICQ and second-order sufficient conditions, which hold generically).
(c) Continuity: $\min$ of continuous functions.
Differentiability: when the minimizer is unique, $c_{\mathrm{EHZ}} =
A(S^*,\sigma^*;\cdot)$ locally, hence smooth by~(b).
Genericity: $A(S_1,\sigma_1;a) = A(S_2,\sigma_2;a)$ is one equation in~$4F$
unknowns, so the tie locus is a hypersurface; finitely many orbit pairs, so
the union has measure zero.
(d) Standard result for the directional derivative of a pointwise minimum
of smooth functions.
\end{proof}
\end{unverified}

% Jörn: math approved (b01bf24) — Remark
\begin{remark}[Simplicity is generic]
\label{rem:simplicity-generic}
A polytope in dual-vertex representation is non-simple when $5$ or more facet
hyperplanes $\langle a_k, x \rangle = 1$ meet at a single point, which
imposes one additional equation beyond the $4$ needed to determine a vertex.
This is a codimension-$1$ condition in the space of dual vertices, so non-simple
polytopes form a measure-zero subset: a random polytope is almost surely simple.
All~$140$ polytopes in our dataset are simple at the start, and the step bound
preserves simplicity (it is a special case of preserving the combinatorial type).
For non-simple polytopes, vertices can split discontinuously under
perturbations of $a_k$, so the combinatorial type need not be locally constant.
\end{remark}

\emph{Validity of the single-orbit gradient.}
In our experiment, we compute $\nabla_a\,c_{\mathrm{EHZ}}$ using only
the current minimizing orbit. By Proposition~\ref{prop:capacity-piecewise-smooth}(c),
this is exact whenever the minimizer is unique.

% [TODO: JÖRN - verify statement — was cor:sys-derivative]
\begin{unverified}
\begin{corollary}[Systolic ratio derivative]
\label{cor:sys-derivative}
The chain rule applied to
$\mathrm{sys} = c_{\mathrm{EHZ}}^2 / (2\,\mathrm{vol})$ gives
\[
  \frac{\partial\,\mathrm{sys}}{\partial a_k}
  = \frac{c_{\mathrm{EHZ}}}{\mathrm{vol}}
    \cdot \frac{\partial\,c_{\mathrm{EHZ}}}{\partial a_k}
  - \frac{\mathrm{sys}}{\mathrm{vol}}
    \cdot \frac{\partial\,\mathrm{vol}}{\partial a_k},
\]
where the partial derivatives of $\mathrm{vol}$ and $c_{\mathrm{EHZ}}$ are
given by Lemmas~\ref{lem:vol-derivative} and~\ref{lem:cap-derivative}.
\end{corollary}
\end{unverified}

% [TODO: JÖRN - step bound uses the linear approximation (ignores t^2 term).
% The quadratic t^2 ω₀(g_i,g_j) term can make the bound non-conservative.
% Verify whether the code accounts for this or whether the bound needs fixing.]
\begin{unverified}
\paragraph{Step bound.}
A gradient step sets $a'_k = a_k + t\,g_k$ where
$g_k = \partial\,\mathrm{sys}/\partial a_k \in \mathbb{R}^4$.
To preserve the combinatorial type, we require a step bound
$t_{\max}$ satisfying:
\begin{itemize}
\item \emph{Vertex feasibility.}
  For a simple vertex $v$ (exactly 4 determining facets with dual vertices
  forming an invertible matrix $A_v$), the vertex moves as
  $v(t) = A_v(t)^{-1}\,\mathbf{1}$.
  For each non-incident facet $j$, the slack
  $s_j(t) = 1 - \langle a_j + t\,g_j,\, v(t) \rangle$
  must remain positive.

\item \emph{Symplectic sign preservation.}
  For each ridge-adjacent pair $(i,j)$, the signed area
  $\omega_0(a_i + t\,g_i,\, a_j + t\,g_j)
  = \omega_0(a_i,a_j) + t\,[\omega_0(g_i,a_j) + \omega_0(a_i,g_j)] + t^2\,\omega_0(g_i,g_j)$
  must not change sign.
  The linear approximation gives
  $t < \omega_0(a_i,a_j)\,/\,\max(0,\;-[\omega_0(g_i,a_j) + \omega_0(a_i,g_j)])$.
\end{itemize}
The step bound is $t_{\max} = \min$ over all such critical times.
\end{unverified}

We evaluate $\mathrm{sys}$ at step fractions
$\{0.1, 0.25, 0.5, 0.75, 0.95\} \cdot t_{\max}$.

The starting polytopes are the 140 polytopes from the random-sweep and
random-product-sweep experiments with $F \le 10$.

\subsubsection*{Phase 1: Gradient Structure}

Figure~\ref{fig:sys-opt-gradient} shows the distribution of the
gradient norms $\|\partial\,\mathrm{sys}/\partial a_k\|$
across all polytopes and facets.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_gradient_hist.png}
\caption{Distribution of $\|\partial\,\mathrm{sys}/\partial a_k\|$
  across 1120 facets from 140 polytopes.
  Left: signed values on linear scale. Right: magnitudes on log scale.}
\label{fig:sys-opt-gradient}
\end{figure}

Figure~\ref{fig:sys-opt-gradient-comparison} compares the gradient
prediction and step bound.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_gradient_comparison.png}
\caption{Left: predicted maximum $\Delta\mathrm{sys}$ from a single step
  ($t_{\max} \cdot \|\nabla_a\,\mathrm{sys}\|$, dimensionless).
  Right: maximum step size $t_{\max}$, constrained by vertex feasibility
  and $\omega_0$ sign preservation.}
\label{fig:sys-opt-gradient-comparison}
\end{figure}

The dimensionless quantity
$t_{\max} \cdot \|\nabla_a\,\mathrm{sys}\|$ --- the predicted maximum
$\Delta\mathrm{sys}$ from a single linear step --- combines gradient
magnitude and step bound into a comparable measure.

\subsubsection*{Phase 2: Finite Gradient Steps}

For each polytope, we take a gradient step along
$g = \nabla_a\,\mathrm{sys}$ and evaluate the improvement.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_improvement.png}
\caption{Left: $\Delta\mathrm{sys}$ from a single gradient step
  for each polytope.
  Right: best achievable $\mathrm{sys}$ vs.\ starting $\mathrm{sys}$.}
\label{fig:sys-opt-improvement}
\end{figure}

\begin{table}[ht]
\centering
\input{sys_optimization_stats.tex}
\caption{Summary statistics for the sys-optimization experiment.}
\label{tab:sys-opt-stats}
\end{table}

Overall, 129/140 polytopes improve under the gradient step.
The best $\mathrm{sys}$ achieved is~$0.805$, starting from a polytope
with $\mathrm{sys} = 0.735$ (a random $4 \times 5$ Lagrangian product).

\subsubsection*{Phase 3: Iterative Gradient Ascent}

Phases~1--2 evaluated single gradient steps from fixed starting polytopes.
Phase~3 iterates: after each gradient step, we recompute HK2017 on the
resulting polytope, compute the new gradient, and take another step.
This allows the procedure to navigate across orbit switches and exploit
the piecewise-smooth structure of $\mathrm{sys}$.

At each iteration, we evaluate 5 step fractions
$\{0.1, 0.25, 0.5, 0.75, 0.95\} \cdot t_{\max}$
and select the step with the highest resulting $\mathrm{sys}$.
Iteration terminates when the best candidate step improves
$\mathrm{sys}$ by less than $10^{-6}$, when no candidate step
succeeds (all constructions fail), or after 20 iterations.
Note that termination does not imply stationarity of $\mathrm{sys}$:
the gradient may still be large, but the type-preserving step bound
may be too tight for any candidate step to produce a measurable
improvement.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_convergence.png}
\caption{Left: $\mathrm{sys}$ trajectories under iterative gradient
  ascent, colored by facet count.
  Right: per-step improvement $\Delta\mathrm{sys}$ (symlog scale).
  Most trajectories terminate within 5--10 iterations.}
\label{fig:sys-opt-convergence}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_iteration_summary.png}
\caption{Left: distribution of iteration counts (mean 6.3).
  Center: starting vs.\ final $\mathrm{sys}$ (most polytopes improve substantially).
  Right: step type usage across all 868 iterations.}
\label{fig:sys-opt-iteration-summary}
\end{figure}

Iteration produces a substantial cumulative improvement:
the mean cumulative $\Delta\mathrm{sys}$ is $0.149$, roughly $3\times$
the single-step improvement of Phase~2.
The best $\mathrm{sys}$ after iteration reaches~$0.878$
(polytope \texttt{random\_5x5\_1}, starting from $\mathrm{sys} = 0.351$).
Note: this is a different polytope from the single-step best ($0.805$);
the highest starting $\mathrm{sys}$ in the dataset is~$0.758$, but that
polytope admits no improving step.
All 138 iterated polytopes terminate with improvement below $10^{-6}$
(2 polytopes admit no improving step from their starting point).

\subsubsection{Phase 4: Gradient Validity Testing}
\label{sec:gradient-validity}

Phases~1--3 treat the analytical gradient as correct and use it
for optimization. Phase~4 tests this assumption empirically:
does the linear prediction
$\mathrm{sys}(a + t\,\delta) \approx
\mathrm{sys}_0 + t\,(\nabla_a\mathrm{sys} \cdot \delta)$
hold in practice?

For each of the 140 polytopes, we test along 12 directions:
the $\nabla_a\,\mathrm{sys}$ gradient direction
and 11 random directions (Gaussian in $\mathbb{R}^{4F}$, normalized).
For each direction, we evaluate at 8 step fractions
$t/t_{\max} \in \{0.01, 0.1, 0.25, 0.5, 1.0, 2.0, 5.0, 10.0\}$,
where $t_{\max}$ is the combinatorial-type-preserving step bound.
Each evaluation constructs the perturbed polytope, runs HK2017, and
compares the actual $\Delta\mathrm{sys}$ against the linear prediction.
This produces 13{,}440 evaluations total.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{sys_optimization_validity.png}
\caption{Left: median relative prediction error vs.\ step fraction,
  by direction type (shading: interquartile range).
  The gradient direction (red) tracks the linear prediction closely
  ($7.3\%$ error at $0.25\,t_{\max}$).
  Random directions (blue) have ${\sim}90\%$ error at all scales
  (the gradient is nearly orthogonal to random perturbations in the
  high-dimensional parameter space).
  Center: construction success rate beyond the step bound.
  85\% of evaluations at $2\times t_{\max}$ produce a valid polytope.
  Right: distribution of the largest step fraction with
  $<20\%$ relative error, by direction type.}
\label{fig:sys-opt-validity}
\end{figure}

Three findings:
\begin{enumerate}
\item \emph{Gradient predictions are accurate.}
  Along $\nabla_a\,\mathrm{sys}$, the median relative error is $0.3\%$
  at $0.01\,t_{\max}$, $2.8\%$ at $0.1\,t_{\max}$, and $7.3\%$ at
  $0.25\,t_{\max}$.
  This is consistent with $O(t)$ truncation error from the first-order
  approximation and validates the analytical derivatives
  (Lemmas~\ref{lem:vol-derivative} and~\ref{lem:cap-derivative}).

\item \emph{Large-step predictions have outliers.}
  At larger step sizes, 15 polytopes have wrong-sign predictions
  (the gradient predicts the opposite direction of actual change),
  and 6 polytopes have prediction ratios exceeding 25:1.
  % Investigation of 4990 validity records found two mechanisms for
  % wrong-sign gradient predictions:
  % (1) At large step sizes (t ≥ 1.0×t_max), polytope combinatorics change
  %     (vertex_count_changed=True in 32 records) — the gradient is meaningless
  %     because the objective function's domain has changed.
  % (2) At moderate step sizes without combinatorial change, Reeb orbit
  %     switching: a different (S,σ) candidate becomes the capacity-achieving
  %     orbit, invalidating the gradient computed for the original orbit.
  % The iterative optimizer (Phase 3) avoids both: its line search stays
  % in the linear regime (t_fraction ≤ 0.95) and recomputes HK2017 after
  % each step, detecting orbit switches immediately.

\item \emph{Step bounds are conservative.}
  At $2\times t_{\max}$, 85\% of evaluations succeed (valid polytope
  constructed), and 35\% also preserve the combinatorial type.
  Even at $10\times t_{\max}$, 60\% of evaluations succeed.
  The bounds are thus safe but overly cautious for polytope
  construction; the binding constraint in practice is
  combinatorial type preservation, not polytope validity.
\end{enumerate}

\subsubsection*{Discussion}

Iterative gradient ascent does not push $\mathrm{sys}$ past~$1$.
The best result ($\mathrm{sys} \approx 0.88$) remains below the
Viterbo threshold, though it substantially exceeds the maximum
$\mathrm{sys} \approx 0.76$ found by random sampling alone.

Three structural observations:
\begin{enumerate}
\item \emph{All 140 starting polytopes have nonzero gradient.}
  None of the 140 starting polytopes is a critical point of $\mathrm{sys}$
  under dual-vertex perturbations; every one admits a direction of improvement.
  After iteration, 138/140 terminate with the best candidate step
  improving $\mathrm{sys}$ by less than~$10^{-6}$.
  This reflects the greedy procedure running out of type-preserving
  steps, not stationarity of~$\mathrm{sys}$.

\item \emph{Iteration produces substantial cumulative improvement.}
  The mean cumulative $\Delta\mathrm{sys}$ of $0.149$ is ${\sim}3\times$
  the single-step improvement, showing that the iterative
  procedure successfully navigates across orbit switches.
  However, the improvement rate decreases rapidly: most of the gain
  occurs in the first 3--5 iterations.

\item \emph{Step bound is the bottleneck, not gradient quality.}
  The gradient accurately predicts $\mathrm{sys}$ within the step bound
  (Phase~4), but the type-preserving step bound --- constrained by
  $\omega_0$ sign preservation on ridge-adjacent facets --- limits
  achievable improvement per step.
\end{enumerate}

\emph{Limitations and extensions.}
The greedy gradient ascent used here preserves the combinatorial type at
each step. Several extensions could push $\mathrm{sys}$ further:
\begin{itemize}
\item \emph{Large steps beyond the gradient regime.}
  Taking steps larger than $t_{\max}$ (accepting a combinatorial type
  change) and recomputing $\mathrm{sys}$ from scratch.
  The gradient prediction is invalid, but the actual $\mathrm{sys}$ can
  still be evaluated.
\item \emph{Targeted single-facet moves.}
  For $k \notin S$ (not in the minimizing orbit), $\partial c_{\mathrm{EHZ}}/\partial a_k = 0$,
  so small dual-vertex changes affect only $\mathrm{vol}$.
  However, large changes in $a_k$ could trigger an orbit switch to a
  better orbit, combining volume reduction with capacity improvement.
\end{itemize}

Phase~4 (Figure~\ref{fig:sys-opt-validity}) addresses the question of
gradient validity: the gradient accurately predicts $\mathrm{sys}$
within the step bound, and beyond the step bound, polytope construction
succeeds far more often than the conservative bounds require.
