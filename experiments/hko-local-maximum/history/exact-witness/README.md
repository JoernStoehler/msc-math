# Exact Witness Execution Notes

This directory contains remaining runnable tooling and generated artifacts for
older active-minima bookkeeping and endpoint/segment prototype context. It is
history material, not the current theorem certificate.

Research interpretation and theorem-facing status live in
`research/hko-local-maximum-exact-witness.md`.

The scripts in this directory write tracked JSON artifacts next to the scripts.
Do not run them as casual smoke checks. Run them only when intentionally
refreshing a specific remaining historical artifact.

There is no default command for this directory. Run a remaining script only
when intentionally checking that specific historical artifact.
