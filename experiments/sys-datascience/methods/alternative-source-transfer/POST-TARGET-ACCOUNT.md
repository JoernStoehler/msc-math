# Alternative-source transfer: accepted post-target account

Status: accepted bounded result; packet closed.

The exact reviewed evaluator output is stored in
[`artifacts/transfer-v1/target-evaluations.jsonl`](artifacts/transfer-v1/target-evaluations.jsonl)
(SHA-256 `6016b66c5cad4af948b6d0188ccfa5f1d455b10093e5e2f61d303401fc0082f5`,
91 unique rows). The frozen analysis is
[`artifacts/transfer-v1/analysis.json`](artifacts/transfer-v1/analysis.json)
(SHA-256 `872b932cf38811184104a6bf46afe34f079c291fa5b6e9bc90e05a80df1d407a`).
The compact provenance and scope record is
[`artifacts/transfer-v1/result-manifest.json`](artifacts/transfer-v1/result-manifest.json).

The result is for one `factorial-both` source, master seed `2026071601`, and
the two fixed buckets `4x6` and `6x6`. The frozen design has shared controls,
five rho/ridge overlaps, and no `sys > 1` rows. The analysis JSON owns the
full bucket-level descriptive table, detailed metric rows, intervals, and
permutation counts. Both selectors received the predeclared
`strong_transfer` label as finite-design enrichers in both tested buckets.

The strongest supported claim is: on this one fresh, separately
area-normalized source, both frozen target-free selectors operationally
enriched mean `sys` over disjoint controls in both tested buckets. This is
finite-design sub-threshold proposer-transfer evidence. The label
`strong_transfer` is the packet's predeclared descriptive classification.

The bootstrap ranges are descriptive finite-panel resampling diagnostics,
conditional on the realized extreme-order selection; they are not population
confidence intervals. The permutation result means no sampled null relabeling
was at least as extreme in 10,000 draws; it is not `p = 0`, a causal
randomization test, or statistical proof. Shared controls and five overlaps
make the arm diagnostics correlated, and there is one source seed and two
fixed buckets.

Do not use this result as evidence for a mechanism, causal support-height
effect, rho superiority over ridge, fresh-seed/bucket/law stability,
population inference, theorem/counterexample status, threshold-finding, or
`sys > 1`. The packet is closed; no automatic follow-up is queued.
