# Rust
/crates/target/
/experiments/target/
**/*.rs.bk

# Python
__pycache__/
*.pyc
.venv/

# LaTeX (thesis)
thesis/build/
thesis/*.aux
thesis/*.log
thesis/*.out
thesis/*.toc
thesis/*.bbl
thesis/*.blg
thesis/*.fdb_latexmk
thesis/*.fls
thesis/*.synctex.gz
thesis/*.pdf

# LaTeX (crate math docs)
crates/src/*.aux
crates/src/*.log
crates/src/*.out
crates/src/*.toc
crates/src/*.pdf
crates/src/**/*.aux
crates/src/**/*.log
crates/src/**/*.out

# Data (regenerated by pipeline)
# experiments/data/ and experiments/figures/ are now tracked
# experiments/viz/ scripts write directly to docs/viz/data/ (already tracked)

# Node (Playwright screenshots, dev tooling)
node_modules/
package.json
package-lock.json

# Claude Code worktrees (created by --worktree / EnterWorktree)
.claude/worktrees/

# Claude Code agent memory (auto-generated, session-specific)
**/.claude/agent-memory/

# Profiling artifacts
perf*.data
perf*.data.old

# Editor
*.swp
