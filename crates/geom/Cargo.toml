[package]
name = "geom"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
nalgebra = { workspace = true }
qhull-sys = "0.4.0"
serde = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
