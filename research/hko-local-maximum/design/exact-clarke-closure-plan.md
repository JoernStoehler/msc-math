# Exact Clarke Checker Closure Plan

## Purpose

Working execution tracker for closing the `TASKS.md` item
`Exact Clarke-subgradient checker for HKO2024`.
This file is the durable session surface for the exact first-order route to the
`RESULTS.md` `M_10` theorem.

## Goal

Close the first-order `M_10` theorem surface:

- prove in dual-vertex coordinates `R^40` that the Clarke-flat directions at
  HKO2024 are exactly the infinitesimal symmetry directions generated by
  translations, scaling, and affine symplectic linear maps;
- produce exact machine-readable artifacts for the active-gradient matrix,
  its kernel, the symmetry tangent space, and exact certificates that the two
  subspaces coincide;
- downgrade the current second-order route to supporting numerical evidence.

## Locked Decisions

- Symmetry surface: use the full natural quotient directions
  `translations + scaling + linear Sp(4)`.
- Field policy: accept the actual smallest exact field needed by the HKO
  coordinates and orbit formulas; do not force the route to remain quadratic.
- Tooling policy: SageMath is the primary exact algebra system.
  The devcontainer should gain a reproducible Sage install surface.
- Input policy: the final theorem-facing orbit catalog must come from the HKO
  geometry / paper route, not from floating-point active-set discovery.
  Numerical artifacts may be used only as cross-checks and bookkeeping aids.

## Current Known State

- The current numerical `a_i`-space route already gives `rank(G) = 25`,
  `dim ker(G) = 15`, and no observed improving first-order direction.
- The committed HKO dual coordinates are already visible in
  `experiments/hko-local-maximum/gradient-analysis/hko-neighborhood-sensitivity.jsonl`
  and `library/src/geom/known_polytopes.rs`.
- The committed HKO dual geometry lives naturally in the quartic field
  `Q(tan(pi/5))`, with minimal polynomial `t^4 - 10 t^2 + 5 = 0`.
- The quartic field is not caused by the `a_i = n_i / h_i` normalization:
  the common support height is quadratic, but the standard-coordinate facet
  normals already leave `Q(sqrt(5))`.
- The repo does not yet contain an explicit `R^40` symmetry tangent basis.
- The repo does not yet contain a paper-derived exact catalog of the minimizing
  orbit families in repo notation.
- The repo currently contains stale count surfaces for the minima:
  `44/10` in older writeups and `150` exact-action numerical orbit payloads
  in the current artifact.
- Current Packet 2 bookkeeping evidence splits those `150` exact minima into
  `44` six-facet endpoint-style orbit records and `106` seven-facet
  equality-case orbit records. There are `10` visited six-facet subsets and
  `10` visited seven-facet subsets; the current numerical gradient classes
  suggest that the seven-facet surface lies on segments between neighboring
  six-facet endpoint classes. The current reconciliation artifact records
  `15` distinct six-facet gradient classes, `13` distinct seven-facet gradient
  classes, and only two observed segment coefficients on the seven-facet
  surface: `1/2` and approximately `0.129573855671`. The ten visited
  six-facet subsets form one orbit under diagonal `72^\circ` rotation plus
  `q/p` exchange, and the ten visited seven-facet subsets do as well.

## Work Packets

### Packet 1 — Exact Geometry And Symmetry Basis

Status: `completed`

Objective:
- write down the exact HKO dual-vertex coordinates in a durable machine-usable
  form;
- identify the actual field;
- derive the explicit dual-coordinate symmetry action and its infinitesimal
  `R^40` tangent basis.

Deliverables:
- exact geometry record;
- exact field record;
- explicit `40 x 15` symmetry tangent matrix with basis labels.
- Implemented artifacts:
  `experiments/hko-local-maximum/exact-clarke/hko-geometry.json`,
  `experiments/hko-local-maximum/exact-clarke/hko-symmetry-tangent.json`.

Checks:
- dual coordinates reproduce the current HKO halfspaces exactly;
- symmetry tangent matrix has exact rank `15`.

Observed result:
- the committed HKO dual geometry lives naturally in the quartic field
  generated by `t = tan(pi/5)`, with minimal polynomial
  `t^4 - 10 t^2 + 5 = 0`;
- the emitted symmetry tangent matrix has exact rank `15`.

Stop condition:
- if the coordinate field or the translation action in dual coordinates is not
  encoded cleanly enough for exact linear algebra, record the obstruction here
  before widening scope.

### Packet 2 — Orbit Catalog Reconciliation

Status: `in_progress`

Objective:
- replace the stale minima count surface with one authoritative theorem-facing
  catalog;
- reconcile the paper’s minimizing billiard-family description with the current
  numerical exact minima.

Deliverables:
- finite catalog in repo notation `(subset, permutation, beta-pattern)` or a
  precise obstruction note;
- written explanation of the current `44/10` versus `150/20/28` mismatch.
- Current bookkeeping artifact:
  `experiments/hko-local-maximum/exact-clarke/numerical-minima-summary.json`.
- Current classification artifact:
  `experiments/hko-local-maximum/exact-clarke/numerical-family-reconciliation.json`.

Checks:
- every catalog family maps to current exact-action numerical minima;
- every current exact-action numerical minimum is explained as either a catalog
  family instance or a duplicate representation.
- if the paper-derived three-bounce equality cases contribute only segment
  interiors between endpoint gradient classes, record that reduction explicitly
  before promoting the endpoint family to theorem input.

Stop condition:
- if the HKO paper does not determine a finite theorem-facing catalog without a
  new derivation program, stop and record that gap explicitly instead of
  silently switching theorem input to numerical discovery.

### Packet 3 — Sage Exact Checker

Status: `pending`

Objective:
- implement the exact checker in SageMath against the exact geometry record and
  the orbit catalog from Packet 2.

Deliverables:
- Sage entrypoint;
- exact active-gradient matrix `G`;
- exact `rank(G)`, basis of `ker(G)`, symmetry tangent basis, and inclusion /
  equality certificates;
- machine-readable certificate output;
- short theorem-facing summary.

Checks:
- exact rerun reproduces `rank(G) = 25` and `dim ker(G) = 15`;
- every symmetry basis vector lies in `ker(G)`;
- exact dimension count proves `ker(G) = Sym`.

Stop condition:
- if Packet 2 is unresolved, do not present Packet 3 as a theorem closure;
  record it as an exact checker over the provisional catalog only.

### Packet 4 — Formal / Thesis Integration

Status: `pending`

Objective:
- switch the thesis/formal result surface from the current second-order route
  to the exact first-order quotient-space route.

Deliverables:
- updated `TASKS.md`;
- updated `RESULTS.md`;
- updated HKO formal/thesis wording.

Checks:
- theorem wording states local maximality modulo symmetries, not strictness in
  raw `R^40`;
- second-order material is clearly supporting evidence only;
- stale minima counts are removed or explicitly labeled as legacy evidence.

## Open Risks

- The orbit catalog may still require fresh mathematical derivation rather than
  bookkeeping extraction.
- The geometry field is confirmed to be the simple quartic
  `Q(tan(pi/5))`, but the orbit-gradient formulas could still force a larger
  extension.
- The quotient theorem still depends on encoding the symmetry tangent space
  correctly in dual-vertex coordinates, especially the translation directions.
- SageMath remains the intended primary CAS surface, but this devcontainer base
  does not currently expose a directly installable `sagemath` apt package.
  Packet 1 therefore uses the committed sympy fallback, while Packet 3 still
  needs an explicit environment choice for the full Sage route.

## Immediate Next Steps

1. Add this tracker to `TASKS.md` as the durable execution surface.
2. Add SageMath to the devcontainer install surface.
3. Build the exact geometry record and symmetry tangent basis.
4. Reconcile the minima count surface before claiming theorem closure.
