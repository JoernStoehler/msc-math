# Supervised And Representation Methods

Status: current cross-packet map. The named retained-table baseline gap is
closed; this file is not a launch queue.

Status block:

- topic-status: named retained-table baseline gap closed
- spawn-status: do not spawn another ordinary-model packet by default
- next-role: topic reviewer only for a named new representation or interface
- next-action: none without a reopen condition below
- review-gate: in-table prediction does not validate a generated-candidate
  proposer
- last-reviewed: 2026-07-23 against `standard-baseline-p2`
- source-of-truth: packet READMEs and the maintained method-disposition table
- stale-if: the retained feature schema, producer population, or named
  downstream claim changes
- allowed-downstream-use: method coverage and claim-boundary navigation

## Current result

`standard-baseline-p2/` completed the previously missing lasso, elastic-net,
gradient-boosting, high-tail classification, and feature-family-ablation
baselines under grouped holdout. Together with the earlier packets, the
retained random/product table now has:

- ridge, lasso, and elastic-net regression;
- random-forest and gradient-boosting regression;
- elastic-net and gradient-boosting high-tail classification;
- shallow tail rules;
- grouped and metadata-only controls;
- combinatorial-count versus symplectic ridge-area feature ablations;
- PCA, clustering, and isolation-style diagnostics.

These are in-table association, ranking, classification, and diagnostic
results. They do not validate a generated-candidate proposer, estimate support
near `sys > 1`, or show that the current 45 invariant features contain the
geometry needed to distinguish the known positive regime.

## Source packets

- `../../methods/standard-baseline-p2/`: the completed named baseline set and
  its reviewed interpretation;
- `../../methods/prediction-ranking/`: ridge, random forest, grouped holdout,
  and metadata-only baselines;
- `../../methods/tail-rule-mining/`: shallow high-tail rules;
- `../../methods/projection-structure/`: PCA, clustering, and anomaly
  diagnostics;
- `../../methods/statistical-associations/`: scalar and grouped association
  checks;
- `../../methods/trusted-random-product-method-dispositions.md`: current
  family-by-family disposition and reopen conditions.

## Reopen conditions

Do not rerun another ordinary retained-table model merely because it is absent
from the list. Reopen this topic only when a named downstream claim needs one
of:

- a genuinely different representation or invariant family;
- a generated-candidate selection rule evaluated before `sys`;
- a calibrated comparison on a new producer population;
- a specific model interface not covered by the current linear, tree,
  classification, rule, projection, or anomaly packets.

Earlier versions of this map proposed P2's methods as missing work. That
proposal is obsolete; Git history retains it.
