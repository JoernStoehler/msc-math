# Sys-Datascience Active Work

Use: short-lived ownership registry, not a backlog or evidence source. Remove
entries after merge, durable parking, discard, or handoff.

## Current Active Entries

- **Tangential-both ridge/rho proposer-transfer freeze** — branch
  `alternative-source-transfer`, worktree `.worktrees/alternative-source-transfer`.
  Owns only `methods/alternative-source-transfer/**` plus its binary
  registration; target-free until independent pre-target review.

- **Alternative-generator and transfer line** — branch
  `research/generator-transfer`, worktree `.worktrees/generator-transfer`.
  Owns generator-law contrasts, transfer interpretation, and integration of its
  bounded child packets.
- **Orientation target pilot** — branch
  `research/generator-orientation-target-pilot`, worktree
  `.worktrees/generator-orientation-target-pilot`. Owns the frozen 24-target
  identity/Haar-`U(2)`/Haar-`SO(4)` paired pilot and its method-local analysis.
- **Generator distribution contract** — branch
  `research/generator-distribution-contract`, worktree
  `.worktrees/generator-distribution-contract`. Owns only
  `methods/generator-distribution-contract/**`; accepted handoff pending line
  integration.
- **Generator target-quotient distance** — branch
  `research/generator-target-quotient-distance`, worktree
  `.worktrees/generator-target-quotient-distance`. Owns only the symplectic-Gram
  proof note and `methods/generator-target-quotient-distance/**`; accepted
  handoff pending line integration.
