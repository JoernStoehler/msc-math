# Sys-Datascience July Recovery Audit

Status: complete file-level audit of checkpoint `bb0e538e` against `main` at
`7ddbb999`. This is recovery provenance, not current research control or
experiment evidence.

Purpose: record which ownerless July files were integrated and preserve the
abandoned agent workflow at its actual status: a half-tested GPT-5.5 prototype,
not current instructions and not a workflow to debug for GPT-5.6.

## Audit Standard

Material was promoted only when it retained information useful after the
exploration decision and had a clear current owner. Historical execution state
was not promoted merely because it represented substantial work. Generated
metrics remain owned by method artifacts; current research direction remains
owned by `exploration-result.md`.

## Integrated Evidence And Code

The initial integration already retained and subsequently reviewed:

- the P2 analyzer, README, review, command record, and compact generated
  artifacts under `../methods/standard-baseline-p2/`;
- source-reproducible P2 input construction and recorded table fingerprints;
- the method disposition and coverage updates that record P2 family status;
- the corrected two-face-control smoke plan, rationale, validator support, and
  dormant LICCA/script classification;
- the source-backed cross-method interpretation and exploration stopping
  decision in `exploration-result.md`;
- bounded thesis chapter and appendix drafts, with their status kept separate
  from final demonstration and writeup.

No experiment code, reviewed P2 output, producer-plan artifact, or unique
current experiment decision from `bb0e538e` remains omitted.

## Preserved GPT-5.5 Workflow Material

The exact prototype remains coherent at checkpoint `bb0e538e`, including its
agent-memory/control document, autonomous parent loop, prompt templates,
coordination state, dated packet syntheses, and material edits made after
probes. The current tree does not copy or repair that instruction stack.

`workflow-evaluations/2026-07-08-parent-loop-probes.md` is retained as a compact
index of what was tested, the six bypasses found, the reported re-probe result,
and the source session/commit pointers. Its status is deliberately only
“half-tested GPT-5.5 material.” It is not evidence that the workflow is suitable
for GPT-5.6 or for the current thesis phase.

Any GPT-5.6-ready workflow should be designed from scratch against the current
model, repository, and observed failures. The GPT-5.5 prototype may be used as
historical input, without treating its state machine, prompts, checklists, or
probe verdicts as requirements.

## Not Promoted: Absorbed Research Synthesis

The following dated syntheses are useful provenance but no longer have unique
current content after comparison with the owning packets, disposition table,
exploration result, and active thesis drafts:

- `bounded-retained-table-source-map-writeup-2026-07-08.md`;
- `first-wave-design-2026-07-08.md`;
- `first-wave-p1-p3-results-2026-07-08.md`;
- `p2-synthesis-2026-07-08.md`;
- `p4-generated-candidate-closure-2026-07-08.md`;
- `p5-mechanism-tail-thesis-use-audit-2026-07-08.md`.

In particular, the P4 frozen-rule rescue boundary and P5 mechanism/tail
caveats survive in `exploration-result.md`, the method dispositions, method
packet owners, and active thesis material. Restoring the dated syntheses would
create additional update surfaces for the same claims.

## Not Promoted: GPT-5.5 Prototype And Superseded Execution State

The following files belong to the half-tested GPT-5.5 prototype or encode an
unselected compute continuation and therefore remain together at `bb0e538e`:

- `agent-memory-and-expansion-plan.md`;
- `coordination/autonomous-parent-loop.md`;
- `coordination/high-complexity-producer-compute-packet-2026-07-08.md`;
- the recovery-only autonomous-parent prompt templates;
- recovery-only active-work, next-session, research-ledger, and topic-launch
  edits whose next action was the high-complexity LICCA run or bounded fallback
  decision.

They are not restored piecemeal because that would produce a hybrid workflow
whose tested object no longer exists. They are not being debugged or migrated
to GPT-5.6 as part of recovery.

The high-complexity packet passed useful local plumbing checks, but later
exploration found no named research purpose for that dataset. Its durable
configuration and validation improvements remain under `../produce/plans/`
and `../produce/`; the direct submission sequence remains historical at
`bb0e538e`.

## Reopen Conditions

Consult the historical files only when a concrete need matches their unique
role, for example:

- a new hypothesis selects the high-complexity producer axes;
- a new GPT-5.6 workflow design wants historical comparison with the exact
  GPT-5.5 prototype, prompts, or probe responses;
- a source audit finds a claim or decision missing from all named current
  owners.

Otherwise proceed from `exploration-result.md` to demonstration/polish. Git
history is sufficient for the non-promoted historical files.
