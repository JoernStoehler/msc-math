# DS evidence closure and scientific account

2026-09-18. This packet is scientific source preparation, not thesis prose or a declaration of human acceptance. Production remains paused. The local audit and delegated checks recover enough evidence for a substantive bounded account without first conducting another large experiment.

## What changed our assessment

The original 14,336-row table is available, and its principal historical association reproduces. Its absence at the main checkout path was an access problem. The stronger claim that its targets were computed by the current certified implementation is still unsupported. These are separate questions.

The source has a second, consequential compatibility issue: the registered June table lacks six later ridge-concentration columns, so the current analyzer correctly refuses it. A historical replay using the exact June 29 analyzer and helper reproduces the original 39-feature screen. Do not overwrite missing fields, weaken current validation, or call this the later current-schema analysis.

Two additional small experiments have complete receipts and replayable analyses, and the optimizer study is already substantial. The scientific account need not be reduced to the ridge correlation. The restricted pentagon ridge identity now gives an exact explanatory example; designed paths elsewhere show why it is not a general mechanism.

## Writer's content packet

1. **Question and baseline.** Search for high systolic ratio using both population-level geometric guidance and local refinement. State the actual distributions: 4,096 generic rows (512 each at 5–12 facets) and 10,240 products (1,024 each in ten polygon-pair buckets, 3≤k≤m≤6), seed 42, support heights [0.8, 1.2], acceptance-conditioned generation. The retained numerical targets contain no value above one and maximum 0.86258589584944. Neither finite sampling nor these historical targets settle Viterbo's conjecture.
2. **Observed structure.** The normalized sum of unsigned symplectic two-face areas is strongly negatively rank-associated with stored sys: Spearman −0.9384368671850424 on this mixture. Pearson −0.2050393154151063 is much weaker, so a linear-law description would misstate the finding. Family-max permutation p=1/201 is a coarse screen across engineered features; it is not a general-law probability, a causal result or an independent validation. Product-minus-generic mean 0.04908416085647144 also warns that source composition matters.
3. **From observation to candidate selection.** Ordinary models/ablations investigate predictive information; frozen target-free selectors test whether that information can enrich newly generated pools. Keep generated geometries distinct from capacity evaluations: the original 100k pool led to 1,675 evaluated selected/control bodies, not 100k evaluations. Explain the successes and bounded transfer in [search-account.md](search-account.md), rather than presenting all methods as failures because none exceeded one.
4. **Exact example and its boundary.** For the rotated regular pentagon product, R=16sin(π/5)cos d and sys·R²=16(3+√5). [ridge-mathematics.md](ridge-mathematics.md) supplies the derivation, definitions and code bridge. This is a later analytic explanation of one family, not evidence that it historically motivated the exploratory screen. Existing 3×6 and 4×4 paths show decreasing R can decrease sys. Keep those informative failures beside the explanatory success.
5. **Interventions and local improvement.** Orientation can improve a fixed selected body, while the new four-block request-allocation comparison favors fresh IID shapes. Tangentialization has heterogeneous effects on 16 paired geometries. Branch-aware search provides another route entirely; seven policies on 64 matched starts yield 448 runs with leading median historical evaluator value 0.984783. Explain the objective's branch structure and preserve the distinction between that historical objective and certified mathematical sys. See [current-panels.md](current-panels.md) and [search-account.md](search-account.md).
6. **Conclusion.** These findings establish useful geometric signals, finite enrichment and finite improvement under named designs. They do not establish a universal ridge objective, globally best algorithm, local maximality of sampled endpoints, or impossibility of a counterexample. This is a scientific result with positive content, not a methods catalogue or failed-search apology.

This is a proposed explanatory order, not reconstructed historical chronology. It should be adapted to the actual chapter boundaries: the optimizer material may remain its own subsection or companion chapter, and ridge interpretation belongs in DS according to recorded feedback.

## What was actually checked

- Found sbx snapshot at `/home/agent/.cache/msc-math/artifacts/polytope-invariant-table/c3042846723dbb89db17f2f39d88004d937e1e790949ff6880188fa89fc1900a/files/polytope-table.jsonl`. Its directory timestamp is September 17 21:54, consistent with earlier materialization; this audit does not infer the exact command from timestamps.
- Recomputed SHA256 and row count remotely, copied bytes locally and rechecked them. Table hash 607c8731fa03d190d497edc3e8f1b4cca88f7d238260cce527680f568bc33d59 and tracked provenance hash 6ff88a5accce9a7ec7e5a494107350b0974b2ce0268ea44caae36a18a7494ef2 match the owning lineage document.
- Copied the verified snapshot and its manifest into the standard host cache under `/home/joern/.cache/msc-math/artifacts/polytope-invariant-table/c3042846723dbb89db17f2f39d88004d937e1e790949ff6880188fa89fc1900a/`. No credentials were read and no network artifact publication was performed.
- Current analyzer stopped with the documented six-column schema error. Extracted analyzer and shared helper from `ea8e2657`, the commit owning the retained summary; executed 200 permutations and 1,000 bootstrap resamples with BLAS/OMP/MKL threads capped at 1. All headline values and Spearman coefficients match exactly. Thirty-one numeric leaves differ, maximum absolute delta 6.83e−13; all nonnumeric leaves match. The retained original is untouched. [replay-receipt.json](replay-receipt.json) lists every difference; [replay-associations/summary.json](replay-associations/summary.json) is the actual new output.
- Independently checked the ridge formula against descriptor implementation and 1001 direct angle sums, max error 3.56e−15.
- Replayed both new panels' analysis/control scripts on copies; output summaries are byte-identical. Checked receipts, available frozen source hashes and input correspondence; see the panel report for the later helper change.
- Wider search claims were source-audited, not rerun. In particular 448 optimizer runs were not recomputed. Existing trained-model and large candidate-selection reports remain inherited evidence, not newly reproduced results.

Reproduce the historical analysis from this repository root after the snapshot is cached:

```sh
sh docs/ds-evidence-closure/replay-historical.sh /tmp/ds-historical-association-replay
```

The wrapper was executed successfully and its output is byte-identical to the first historical replay. It checks both input hashes, extracts historical code from Git, uses a temporary directory and cleans its intermediates. The designated output is retained. On another machine materialize the registered snapshot using `scripts/artifacts.py materialize polytope-invariant-table --no-link` first. That retrieval command was not rerun here; SSH recovery was sufficient.

## Remaining obligations and smallest useful next work

| Obligation | Current disposition | Minimal next step and consequence |
|---|---|---|
| Historical association support | Recovered and reproduced at its original schema | Ready for qualified writing; no new population needed to report it. |
| Historical evaluator/run receipts | Not recovered; source filenames/backend labels cannot establish actual cached computation path | Preserve numerical-lineage limitation. If the chapter needs current-evaluator confirmation, select a frozen stratified subset and re-evaluate its stored geometry before advertising that claim. |
| Source geometry/current 45-feature table | Registered source dataset byte IDs exist; source payloads/current-schema rebuilt table were not located in searched sbx/home/tmp paths | Retrieve the two source datasets if a downstream operation needs them. The current-schema rebuild is documented as expensive and previously objectionable at 11cores; do not casually rerun it. Historical 39-feature exposition does not depend on this rebuild. |
| Fresh principal-association replication | Not performed;64/124 intervention requests are not it | Optional if qualified historical observation suffices, necessary if claiming independent current-code replication. Freeze seed, populations, feature ordering checks and analysis before evaluating. Start with a small timed representative pilot; do not extrapolate small 3×3/4×4 timings to all larger buckets. |
| Current numerical panel preservation | Complete packets exist but are untracked in main | Deliberately integrate the three packets and provenance before PDF release; this audit did not commit unrelated main work. |
| New ratio certification | Capacity guarantees exceed the f64 volume guarantee | Do not claim certified ratio intervals. Only add exact/interval volume work if a threshold-level mathematical claim actually requires it. |
| Scientific scope | Enough evidence for substantial bounded account; historical broader scope remains unresolved | Write the actual result structure above and expose specific omitted worthwhile questions. Do not infer a 100-method quota or declare full research closure from this audit. |
| Whole-thesis integration | Prepared facts span DS, pentagon, QP and optimizer text | Update companion claims and retain chapter-specific guarantees. No manuscript was changed here. |

A fresh primary panel is not the next automatic action. First decide the intended scientific statement using this packet. The existing data can support exploratory evidence and the exact family result now. If current implementation corroboration materially improves the account, a small frozen re-evaluation is more directly informative about legacy-target risk than another unrelated small experiment. A wholly fresh population answers a different question: transfer to newly drawn bodies.

## Work and ownership

This isolated worktree is active retained research: `/workspaces/msc-math/.worktrees/ds-evidence-closure`, branch `research/ds-evidence-closure`, based on `e33c3f0f`. No production thesis or main checkout was edited. Three independent subagents checked ridge mathematics, current panels, and the broader search account. No fresh expensive capacity computation, university submission, publication, or restart of the production budget occurred. This turn did not instrument token cost or exact end-to-end walltime; no fabricated cost estimate is supplied.
