# Sys-Datascience Coordination

This directory contains both the current handoff between data-science phases
and coordination material preserved from `main`. It is not a second source for
experiment results.

Current phase: exploration is paused at a Jörn terminology/role crux after the
reviewed independent ridge-concentration packet validated sub-threshold
enrichment. Read `research-ledger.md` and the packet review under
`../methods/extreme-scalar-rejection-proposer/artifacts/100k-ridge-concentration-validation/`.
Generated metrics and packet-local interpretation remain under `../methods/`;
producer and prepared-table contracts remain under `../produce/` and
`../prepare/`.

`feature-completion-contract.md` is a recovery-era launch hypothesis superseded
as current routing by the independent validation result. It remains useful as a
later demonstration-readiness checklist after the exploration crux is settled.

`exploration-result.md` records the 2026-07-10 recovery interpretation; it no
longer governs phase routing. The other coordination files preserve prior
workflow design, process learnings, prompt templates, topic maps, and planning
state from `main`; they may remain useful, but their launch recommendations are
not current execution authority unless a new phase explicitly adopts them.

`recovery-audit-2026-07-10.md` records the completed file-level audit of the
additional July checkpoint. `workflow-evaluations/` indexes the half-tested
GPT-5.5 workflow prototype without making it current launch state.

The additional July 2026 recovered checkpoint at `bb0e538e` also contained
autonomous-loop instructions, workflow probes, intermediate wave syntheses,
and proposed compute packets. Its reviewed P2 evidence, current exploration
boundaries and compact workflow-evaluation index are integrated. The audit
records Jörn's decision to leave the GPT-5.5 workflow coherent at its source
commit and design any GPT-5.6 workflow from scratch.
