# Sys-Datascience P5 Mechanism/Tail Thesis-Use Audit, 2026-07-08

Use: source-map audit for thesis-safe mechanism and tail wording after P2. This
is not thesis prose and not a new experiment. Generated metrics remain
source-truthed in the method packet artifacts named below.

Inputs read:

- `p2-synthesis-2026-07-08.md`;
- `topics/geometric-feature-mechanisms.md`;
- `topics/tail-rare-event-modeling.md`;
- `../methods/standard-baseline-p2/README.md`;
- `../methods/standard-baseline-p2/review.md`;
- `../methods/ridge-mechanism-discriminator/README.md`;
- `../methods/ridge-mechanism-discriminator/artifacts/current/hypothesis_rollup.tsv`;
- `../methods/tail-survival-1m-posterior/README.md`;
- `../methods/tail-survival-1m-posterior/artifacts/decision-summary.tsv`;
- `../methods/tail-survival-1m-posterior/artifacts/model-class-ledger.tsv`.

## Safe Wording Inventory

| Candidate statement | Status | Source map | Boundary |
| --- | --- | --- | --- |
| The retained random/product table contains no trusted `sys > 1` row; max observed `sys` is `0.86258589584944`. | source-backed fact | `standard-baseline-p2/artifacts/summary.json`; `tail-survival-1m-posterior/artifacts/summary.json`; scan/trusted packets | Only the retained random/product table and current prepared run. |
| P2 ordinary retained-table baselines found strong in-table signal but no positive row and no generated-candidate proposer. | reviewed empirical inference | `standard-baseline-p2/review.md`; `p2-synthesis-2026-07-08.md` | In-table only; all rows already evaluated. |
| Ridge symplectic two-face area summaries carry most of the held-out P2 signal under the grouped split. | reviewed empirical inference | P2 feature-family ablation; `p2-synthesis-2026-07-08.md` | Split-specific and diagnostic; not a mechanism theorem. |
| Low ridge magnitude is a real bucket-local empirical signal with controls caveat. | diagnostic mechanism evidence | `ridge-mechanism-discriminator/artifacts/current/hypothesis_rollup.tsv` | Say bucket-local empirical signal, not pooled causality. |
| Product/combinatorial structure is a major confounder, but a pure proxy story is too strong. | diagnostic mechanism evidence | `ridge-mechanism-discriminator/artifacts/current/hypothesis_rollup.tsv` and `ridge_effect_discriminator.tsv` | Does not isolate a theorem-level mechanism. |
| Ridge concentration/distribution features are plausible diagnostic seeds. | diagnostic-only seed | `ridge-mechanism-discriminator/artifacts/current/hypothesis_rollup.tsv` | Mined after `sys`; not proposer validation. |
| Small-area-fraction features are currently weak or ambiguous. | diagnostic-only negative | `ridge-mechanism-discriminator/artifacts/current/hypothesis_rollup.tsv` | Only for current feature definitions and splits. |
| Extreme low-ridge filtering enriches high `sys` but shows Goodharting caution: selected maxima remain below 1. | diagnostic-only caution | `ridge-mechanism-discriminator/README.md`; `hypothesis_rollup.tsv` | Does not prove all low-ridge search fails. |
| Tail and zero-positive estimates do not justify a calibrated blind 1M hit-rate claim. | reviewed scale-up boundary | `tail-survival-1m-posterior/artifacts/decision-summary.tsv`; `model-class-ledger.tsv` | Model-sensitive; use as caveat or compute-decision support. |
| A 1M accepted-sample run is rational only as a deliberately model-dominated probe or after focused/generator-axis work. | reviewed scale-up boundary | `tail-survival-1m-posterior/README.md`; `decision-summary.tsv` | Decision guidance, not probability theorem. |

## Unsafe Or Omit

Do not write:

- "black-box data science found no counterexample source" without naming the
  retained random/product table, method packets, and omitted surfaces;
- "standard data science was exhausted" or "the standard toolbox failed" as a
  universal claim;
- "random polytopes do not produce counterexamples";
- "ridge features explain the mechanism" as a theorem or causal claim;
- "ridge concentration validates a proposer";
- "the probability of `sys > 1` under the generator is X";
- "1M samples would/would not find a hit" as a calibrated forecast.

## Thesis-Safe Mechanism Paragraph Shape

Safe content:

> The retained table and generated-candidate diagnostics repeatedly point to
> symplectic two-face area summaries as useful empirical coordinates for high
> `sys`. In the P2 retained-table baselines, ridge symplectic-area features
> almost entirely explain the held-out predictive signal under the grouped
> split. The mechanism-discriminator packet supports a narrower reading:
> low ridge magnitude is a bucket-local empirical signal, while product and
> combinatorial structure remain major confounders. Concentration summaries are
> diagnostic seeds, not validated proposers.

Required caveat immediately nearby:

> These are empirical diagnostics of the sampled tables and generated-candidate
> packets. They do not prove a symplectic-geometric mechanism and do not by
> themselves select unevaluated candidates before `sys` evaluation.

## Thesis-Safe Tail Paragraph Shape

Safe content:

> The negative search result should be stated as a finite retained-table fact
> and a method-packet result, not as a calibrated density estimate. The tail
> packet shows that zero-positive posterior predictions are prior-sensitive and
> that tail extrapolations disagree by model class. Pooled exponential-excess
> fits make a 1M blind run look attractive, but they fail most observed-tail
> backtests; GPD-style fits keep bounded-tail hypotheses live but are
> threshold-sensitive.

Required caveat immediately nearby:

> Consequently, a larger blind run would be a model-dominated probe, not a
> well-calibrated hit search. Stronger generator-axis or focused proposer work
> would be needed before writing calibrated hit-rate language.

## Parent Interpretation

After P2 and P5, the retained-table story is much closer to thesis-use ready:

- method coverage is stronger because P2 closed the compact missing executor;
- mechanism interpretation has a safe empirical form centered on ridge
  symplectic two-face area summaries;
- tail language has a clear negative boundary against calibrated density or
  blind 1M hit-rate claims.

The remaining full-slice decisions are not data-cleanup issues. They are claim
strength choices:

1. Write the bounded retained-table story with explicit omissions.
2. Reopen P4 only if thesis narrative needs a generated-candidate proposer
   closure/rescue beyond current boundary evidence.
3. Prepare the high-complexity producer compute packet only if broader
   producer/distribution wording is still worth the compute and review cost.

## Next Ranking

Superseded continuation note: this was the ranking immediately after P5. Later
on 2026-07-08, the bounded retained-table source map, P4 proposer closure, and
high-complexity producer compute packet were all written. Use
`next-session-candidates.md` for the current decision board.

The P5-time ranking was:

1. Bounded retained-table source-map/writeup packet if the parent wants to turn
   current evidence into thesis text. It must include omitted-surface warnings.
2. P4 proposer closure/rescue design only if writeup needs stronger
   candidate-proposer closure than "no validated proposer in named packets."
3. High-complexity bucket-extension producer packet conditional on a
   compute/value decision.

Current state after the later packets: no Jörn input is needed for local
inspection/review. Actual high-complexity producer execution requires a LICCA
operator; the packet is not evidence until executed and reviewed.
