# Routing report

The decomposition is numerically checked on all 200 generic rows and 142
retained product `5x5` rows. `sum(E*kappa)=sum(A_symp)` has maximum relative
error below `2e-16`; the computed normalized means/sums agree with the source
feature values to below `8e-15`. Incidence-derived ridge counts agree with the
source counts, every product row has exactly 25 mixed and 10 structural-zero
faces, and translation/positive-scaling checks are below `5e-11` absolute area
error. `validation.json` records the exact counts and tolerances.

The generic selected panel has a strong coarse transfer but no interior
hardening. Its low ordinary Euclidean ridge area, together with lower
Euclidean-area-weighted `kappa`, makes the ordinary-morphology gate the leading
account in this packet. Product rho-only and ridge-only arms both beat their
frozen controls, but their overlap is smaller and not stronger; this is a
discordant two-phenotype observation, not authorization to tune an intersection.

Routing:

- Do not run another generic scalar decade or a new target-bearing rho arm.
- If the line reopens, preserve the source-preserving ordinary-area comparison
  and ask the generator-transfer owner for one controlled orientation test;
  this packet does not implement that intervention.
- Hand the bounce line nothing from this packet yet: the available action
  artifacts lack a source-identifiable row join to these random `5x5` IDs.
- If a future fixed matching/stratification collapses both arms, stop the
  mechanism line with the supported statement “strong coarse enrichment, no
  supported scalar mechanism.”
